# Journal del dev-loop — devtools

> Memoria del bucle entre sesiones. Append cronológico; una entrada por evento (tarea iniciada/cerrada, bloqueo, parada, decisión de arnés, cierre de fase, hotfix). **Escribe para el agente que retomará el trabajo sin tu contexto**: lo no obvio, las decisiones y sus porqués, las deudas — no un log de comandos. Nunca se reescriben entradas antiguas.

## Formatos de entrada

```markdown
## <YYYY-MM-DD> · ⏳ T<ID> iniciada
- 1-2 líneas: qué entrega y coste esperado. (Se escribe en el SELECT del ciclo.)

## <YYYY-MM-DD> · T<ID> cerrada — PASS
- Coste: $X (vs estimado $Y) · Ciclos verifier: N · Tests: N · Commit: <sha-corto> · Evidencia: docs/verifications/T<ID>/
- Decisiones no obvias que las siguientes tareas deben heredar: <1-3 líneas>
- Deuda anotada: <o "—">

## <YYYY-MM-DD> · arnés: <qué cambió>
- Qué pieza del arnés se corrigió (skill/agente/hook/gate), por qué (el ciclo que reveló la carencia) y desde cuándo aplica. El arnés evoluciona deliberadamente, nunca por deriva.

## <YYYY-MM-DD> · T<ID> cerrada — PASS · FASE F<N> COMPLETA
- Lo del cierre normal + resumen de fase: qué entrega observable quedó viva, commits de la fase, deudas que pasan a la siguiente, READMEs revisados. Termina en parada: esperar OK del usuario.

## <YYYY-MM-DD> · hotfix: <síntoma>
- Trabajo fuera del planning (bug en producción/uso real): causa raíz, fix, test permanente que lo protege, y si destapa deuda mayor → candidata a fase de deuda (F<N>b) acordada con el usuario.
```

---

## 2026-07-17 · Proyecto bootstrapeado

PRD v1.0 aprobado el 2026-07-16 (subido a v1.1 el 2026-07-17, ver más abajo); planning aprobado el 2026-07-17. Próxima tarea elegible: **T0.1**.

**Qué es esto, y por qué importa para todas las decisiones siguientes**
- El usuario declaró que devtools es, ante todo, un **banco de pruebas del arnés** (decisión D1 del PRD). No se busca tracción ni monetización. Por eso **no se hizo deep research** y las dos tesis de mercado del §1 quedan `[verificar]` para siempre — no hay tarea que las cierre (regla 3 del planning lo dice explícitamente).
- La idea inicial del usuario era "una suite de las típicas utilidades de developer". Se le devolvió que una suite son N features y que el arnés está construido para una sola. Eligió el reencuadre: **un único campo que autodetecta y encadena** (D2, D3). El catálogo de utilidades independientes quedó como no-objetivo explícito.
- **D5 (historial en servidor con cuenta) se eligió a sabiendas de que es peor producto**: se le advirtió que un devtools que pide cuenta para guardar historial añade fricción, y que guardar lo que la gente pega significa guardar sus tokens. Lo eligió igual porque ejercita la capa de datos del arnés, que es el objetivo. La mitigación es **D7**: no se persiste el input crudo, solo una vista previa truncada y redactada. Si alguien "simplifica" D7 en una tarea futura, convierte el producto en el pasivo que R2 describe.
- **D6 contradice el default del template**: el módulo de auth protege todo salvo login/health; aquí el middleware protege ÚNICAMENTE `/history` y `/api/history`. `/` y `/api/analyze` son públicas. T0.4 lleva un test que falla si alguien protege `/` por inercia.

**Módulos de F0 elegidos** (menú cruzado contra el PRD; regla: si el PRD no lo exige, fuera)
- Entran: esqueleto+gate (T0.1), Docker+Postgres (T0.2), Drizzle (T0.3), auth multi-usuario (T0.4).
- **Fuera: worker/pg-boss, máquina de estados, checkpoints, sweeper, SSE, storage, spend ledger, credenciales cifradas.** Justificado en §5.2 del PRD: una cadena se resuelve en milisegundos de CPU, no hay proceso persistido ni progreso que emitir. Es tentador meterlos "porque es un banco de pruebas y hay que ejercitarlos", y por eso está anotado como **riesgo R6**: sería esqueleto muerto que el gate mantiene para nadie. Cualquier módulo nuevo exige cambio de PRD, no un atajo en el planning.
- **Dos desviaciones del menú, aprobadas por el usuario**: (1) el módulo "UI principal v1" NO va en F0 — la pantalla principal *es* la feature y su motor nace en F1; montarla en F0 contra un motor de pega y recablearla después es churn. (2) el deploy NO va en F0 sino en fase propia (F3): desplegar un cascarón no verifica nada. **El coste asumido de (2)**: las roturas exclusivas de producción (el gotcha de `next start` vs `next dev`) se descubrirán al final.

**Design system y mockups**
- DS: `https://claude.ai/design/p/9d6b478a-191a-4d6c-8071-441488dd195f`. Ya es específico de este producto: `StepCard`, `ChainSummary`, `ConfidenceBar` y `HistoryRow` existen porque el PRD los pide. Eso hizo que TD.2–TD.5 se pudieran repartir contra el inventario real y no contra una plantilla genérica.
- Mockups: `https://claude.ai/design/p/1132e88c-090e-42ad-a121-490714cf7ec5`. **Variante elegida: A "Claro"**. Cuatro pantallas: `FieldClaro` → `/`, `HistoryClaro` → `/history`, `LoginClaro` → `/login`, `SignupClaro` → `/signup`.
- **Desviación deliberada del patrón TD**: TD.1 se encarga también del espejo de `docs/mockups/`. Motivo: los mockups son JSX que necesitan el bundle del DS para renderizar; fabricarlos a mano en el bootstrap habría sido inventar el diseño. TD.1 es la primera tarea que tiene `DesignSync` y el DS en la mano, y la regla 7 exige el mockup local antes de T0.4.
- **Pendiente en TD.1**: confirmar si `variant-mobile.jsx` es de la variante A o de la B (anotado en `docs/mockups/README.md`).
- **Consecuencia de orden que conviene recordar**: el grafo obliga a T0.1 → TD completa → T0.2. La primera parada de revisión humana (el OK visual de TD.7) llega muy pronto.

**El VPS: lo que se descubrió inspeccionándolo (el bucle corre DENTRO del VPS)**
- Topología real, verificada contra `~/infra/caddy` y el proyecto vecino `ugc-factory`: Cloudflare (proxy naranja, SSL Full strict) → Caddy central `edge-caddy` en **`network_mode: host`** → `127.0.0.1:<puerto>`. La fuente de verdad operativa es **`~/AGENTS.md` del VPS**: léelo antes de tocar producción.
- **Bloque de puertos reservado para devtools: 3110–3119 (web=3110)**, según el registro de `~/AGENTS.md` §3. T3.1 debe registrarlo allí en el mismo cambio — ese fichero lo exige.
- **Nunca publicar en `0.0.0.0`**: un puerto abierto por Docker se salta UFW (Docker escribe iptables por debajo del firewall). Solo `127.0.0.1:3110`.
- **Trust boundary**: hay DOS proxies delante. El vecino ya lo resolvió con `header_up X-Forwarded-For {client_ip}` + `TRUST_PROXY=1`, pero con Cloudflare en proxy naranja **eso da la IP de Cloudflare**: la real llega en `CF-Connecting-IP`. Afecta a los dos rate limits (login de T0.4 y `/api/analyze` de T1.4); se cierra en T3.1.
- Por esto el **PRD subió a v1.1** (2026-07-17): §10 y §11 se corrigieron contra el terreno. El §10 original daba VPS y DNS por pendientes de contratar y ambos existían ya. Sin cambio de alcance.
- El directorio del repo se renombró de `~/projects/test-project` a **`~/projects/devtools`** para cumplir la convención `~/projects/<proyecto>` de `~/AGENTS.md` y para que la autodetección local/remote de la skill `deploy` funcione por "ruta del repo == REMOTE_DIR".

**Deudas del arnés detectadas en el bootstrap** (ninguna se tocó: la skill bootstrap prohíbe modificar `scripts/`, hooks y skills)
1. **`scripts/readme-status.mjs` no ve la fase TD.** Detecta fases con `^## (F[\dA-Za-z]+) — (.+)$`, que exige prefijo `F`; `## TD — Design system` no casa y `current` se pone a null, así que sus **7 tareas no se cuentan**. La portada dice "0 de 18" cuando el planning tiene 25. Que el regex de *tareas* sí contemple `TD.1` (con un comentario que lo menciona) indica que es un descuido, no una decisión. Anotado también como nota visible en el README para que la portada no engañe.
2. **`redeploy.sh` asume una red docker `edge` que este VPS no usa.** Hace `docker network create edge` + `docker network connect edge edge-caddy`, y `edge-caddy` corre en modo host: un contenedor en host **no se puede conectar a una red bridge**. La rama es inerte (falla con `warn`, no rompe el deploy). La vía correcta —loopback— la contempla la propia SKILL.md. Configurado en `deploy.env` como `CADDY_UPSTREAM="127.0.0.1:3110"` y documentado allí.
3. **Sin CI todavía**: el repo se creó en el bootstrap (`carlosvillu/devtools`, público). Activar CI cuando haya `package.json` (T0.1 en adelante).

**Decisión pendiente para el usuario (no bloquea T0.1)**
- **AGPL §13 y el enlace a fuente**: devtools es una webapp bajo AGPL-3.0. La §13 obliga a quien despliegue una versión *modificada* a ofrecer su código a los usuarios que interactúen por red — lo habitual es un enlace "Source" visible en la UI. Los mockups de la variante A no lo llevan. No se añadió nada al planning para no meter alcance sin aprobar; si el usuario lo quiere, es un cambio menor en el layout de T1.5 (y en el mockup, que manda).

**Coste del bootstrap**: solo agentes (sin APIs de pago; D8). Ninguna tarea del planning lleva `Coste estimado` por el mismo motivo — si alguna llegara a necesitar una API de pago, es cambio de alcance (regla 6), no improvisación de presupuesto.

## 2026-07-17 · arnés: la skill deploy y readme-status, corregidos en el template y sincronizados aquí

Las tres deudas que dejó anotadas el bootstrap están **pagadas**: se arreglaron en `web-template` (commit `b3f2916`, pusheado) y se sincronizaron a este repo. Lo que reveló la carencia fue el propio bootstrap: tuve que esquivar a mano todo lo de abajo para escribir T3.1, que es exactamente la señal de que el arnés estaba mal, no el proyecto.

**Causa raíz**: la skill `deploy` se escribió contra un VPS imaginado y **nunca contra `~/AGENTS.md`**, que es donde el VPS documenta su propia realidad. Ahora la §Topología apunta allí como fuente de verdad. Si vuelves a encontrar un desajuste entre skill y VPS, ese es el sitio donde mirar primero.

**Qué cambió (todo verificado, no deducido)**
1. **La red `edge` no existe ni puede existir.** `redeploy.sh` hacía `docker network create edge` + `docker network connect edge edge-caddy` en cada deploy. Probado en el VPS: docker lo rechaza — *"container sharing network namespace with another container or host cannot be connected to any other network"* — porque `edge-caddy` corre en `network_mode: host`. Un `warn` lo tapaba. Y con el default `CADDY_UPSTREAM="web:3000"` el site file generado **nunca habría resuelto**: el dominio no enrutaría. Paso eliminado (5 → 4).
2. **`WEB_PORT` es ahora obligatorio** en `deploy.env` (falla rápido con mensaje claro) y `CADDY_UPSTREAM` deriva de él. Aquí vale **3110** (bloque 3110–3119 de `~/AGENTS.md` §3). Antes yo lo tenía escondido dentro de `CADDY_UPSTREAM`, que era un apaño.
3. **El site file generado ya lleva `header_up X-Forwarded-For {client_ip}`.** La SKILL afirmaba que Caddy sobrescribía el header; el fichero que generaba no lo hacía. El vecino lo había añadido a mano en producción y el template nunca aprendió.
4. **Cloudflare y `CF-Connecting-IP` documentados** en la skill `deploy` y en `f0-modules.md`. Consecuencia directa para **T3.1**: con proxy naranja hay DOS proxies, `x-forwarded-for` trae la IP de Cloudflare y la real va en `CF-Connecting-IP`. **Esto ya está vivo como bug en el proyecto vecino** (`carlosvillu/ugc-factory#2`): su rate limit de login agrupa a todos los usuarios por IP de borde de Cloudflare. Evidencia en el issue.
5. **`readme-status.mjs` ya ve las fases transversales**: los dos regex comparten un `PHASE_ID` que acepta `F0..Fn` y `T<LETRA>`. Verificado contra este planning: **0/25 con las 5 fases**, TD incluida. La portada ya no miente y la nota de aviso del README se ha retirado.

**Cambio de infraestructura, no del arnés**: los remotes de `web-template` y `devtools` pasaron de HTTPS a SSH (`git@github.com:…`), como ya tenía ugc-factory. Motivo: no hay credential helper para HTTPS en este VPS, así que **el `git push` del bucle habría fallado en la primera tarea** — el push del bootstrap coló solo porque lo hizo `gh` con su propia auth, no `git`. Con SSH está probado que funciona.

**Sigue pendiente (no se tocó)**: las skills del proyecto (`deploy`, `frontend`, `testing`…) conservan `{{PROJECT_NAME}}` sin sustituir en sus descripciones — el bootstrap solo rellena placeholders en `CLAUDE.md`, `AGENTS.md` y `README.md`. Es cosmético (afecta a cómo se listan las skills), pero es deuda real del template.

## 2026-07-17 · ⏳ T0.1 iniciada

Primera tarea del proyecto: monorepo pnpm (`apps/web`, `packages/core`, `packages/db`), tooling compartido, logger pino con `request_id`, `pnpm gate` con los 6 pasos y `/api/health`. Coste esperado: solo agentes (D8, sin APIs de pago).

## 2026-07-17 · T0.1 cerrada — PASS

- Coste: $0 (vs estimado $0; D8: sin APIs de pago) · Ciclos verifier: **2 (FAIL → fix → PASS)** · Tests: 43 en 6 ficheros · Evidencia: `docs/verifications/T0.1/` (conserva el ciclo FAIL→PASS entero: el FAIL es la prueba de que la verificación muerde)

**Decisiones no obvias que heredan las siguientes tareas**
- **Versiones**: Next 16.2.10, React 19.2.7, TypeScript ~5.9 (existe TS 7, pero typescript-eslint 8 declara peer `<6.1.0`), ESLint 9.39.5 (existe 10; eslint-config-next 16 no lo declara probado), zod ^4, pino ^10. Lo compartido va por catalogs.
- **`withRoute(handler, { route, … })`**: el parámetro `route` es un añadido sobre `api.md` (que pasa solo `schemas`). Lo necesita el child logger para tener un nombre estable de ruta. **Todo handler nuevo debe pasarlo.**
- **La opacidad del 5xx es propiedad del STATUS, no orden de ramas** (`errors.ts`): opaco ⟺ `status >= 500`, detalle SOLO al log. Nació de un bug real (ver incidente 2). No lo conviertas otra vez en una rama.
- **`packages/db` nace vacío** (`export {}`) y **no declara `@app/core`** todavía: sería dep huérfana y knip la caza. T0.3 la añade.
- **`HealthSchema` es hoy `{ok: literal true}`; T0.2 le añade `db: boolean`** (PRD §9). El acoplamiento core↔web que sostiene el control negativo de la Verificación vive en `const body: Health` de `route.ts` — no lo quites.
- **`@app/test-utils` tiene `capture-logs`**: es LA costura con la que se prueba §11. Úsala; no te fabriques otra (`testing/SKILL.md:96` lo veta).
- **`knip.json` ignora `pino-pretty`**: knip no ve un `target: '<string>'`. Está acotado y apunta al test que sí la protege (verificado: quitando el ignore, knip tumba el gate; mutando el target, 3 tests rojos).
- **`pnpm gate` solo existe en la raíz.** Desde un paquete falla con `Command "gate" not found`, que no es un error útil. Ejecuta siempre desde la raíz.

**Incidente 1 — EL ARNÉS MÁS CÓMODO QUE LA REALIDAD (el FAIL del verifier)**
`pino-pretty` no estaba declarado en ningún `package.json`, pero el logger pide `transport: {target:'pino-pretty'}` cuando `NODE_ENV === 'development'` — lo que pone `next dev`. `makeLogger()` lanzaba al construirse ⇒ **HTTP 500 en TODA ruta que pasara por `withRoute`, en desarrollo**. `next start` funcionaba: el fallo era **dev-only**.
La suite estaba **VERDE** todo el tiempo. El mecanismo del engaño, y esto es lo generalizable: `route.test.ts` se declaraba a sí mismo «la conservación permanente de esta cláusula, no solo un curl one-shot del verifier», pero llamaba a `GET()` **en proceso y bajo `NODE_ENV=test`**, donde `pretty=false` y el transport roto no se construye jamás. Test verde y endpoint 500 conviviendo sin contradicción, porque **ninguna rama `pretty:true` se instanciaba en toda la suite**.
→ **Regla derivada (añadida a la skill `testing` y a `dev-loop` en esta misma sesión)**: la conservación permanente de una cláusula (regla 8 del planning) tiene que **ejercitar la MISMA capa que la Verificación**. Si la Verificación dice «curl contra el servidor levantado», un test que llama al handler en proceso NO la conserva — y encima miente diciendo que sí.
→ Efecto colateral bueno: `bootstrapErrorResponse` convirtió el crash crudo de Next en un 500 con envelope + traza por stderr. Por eso el diagnóstico fue de 30 segundos.

**Incidente 2 — el 500 «opaco» no lo era** (lo cazó `simplify`, verificado ejecutando el wrapper)
`AppError('internal', msg, details)` salía por la rama `instanceof AppError` y serializaba `message` y `details` **verbatim al cliente**, esquivando el fallback opaco: el body llegó a llevar una connstring. `architecture.md` §5 es vinculante («el detalle va SOLO al log»). No filtró nada porque los mensajes eran literales nuestros, pero **el primer throw site vivo de `AppError('internal', …)` lo creó un fix pedido en el propio REVIEW de esta tarea** — el hueco pasó de teórico a portante en el mismo ciclo. En F1, `throw new AppError('internal', \`no se pudo parsear ${input}\`)` habría mandado el input del usuario AL CLIENTE, por encima de `REDACT_PATHS` (que solo cubre logs).

**Incidente 3 — la captura de logs que miente VACÍA** (corrige un hallazgo MÍO que era falso)
El bucle afirmó que pino con `transport` + `destination` **lanza**. Es **FALSO**, verificado por el implementer y confirmado por el bucle: pino **no lanza — el transport gana y el destination se descarta en silencio (0 líneas capturadas)** mientras el dato sale por stdout. Consecuencia directa: **cualquier test que combine ambos y afirme `not.toContain('secreto')` pasa VACUAMENTE**, y son justo los tests que protegen §11. Hoy no ocurre (`captureLogs()` nunca pasa `pretty`, todas las llamadas van a `trace`, y el test de §11 de `with-route.test.ts` lleva guard `expect(captured.lines.length).toBeGreaterThan(0)`).
→ Lección de método, no solo de pino: **el bucle trasladó un diagnóstico sin verificarlo y el implementer hizo bien en testearlo en vez de creérselo.** Un assert negativo (`not.toContain`) sin un assert positivo que exija captura no vacía no prueba nada.

**Hueco conocido y documentado (NO es deuda pendiente: es contrato)**
`err.message`/`err.stack` **NO los cubre la redaction** de pino. Verificado: V8 mete un prefijo de 10 caracteres del input en los mensajes de `JSON.parse` (`"eyJhbGciOi"... is not valid JSON`). Está escrito en `redact.ts` y `logger.ts`, y **fijado por tests en core** que se ponen rojos si alguien añade un serializer que scrubee (cambiar la política es visible, no silencioso). Por eso `readJson` distingue el TIPO del fallo (`err_name`) y **jamás** su mensaje.

**Deuda anotada**
1. **Para T1.4 (la tarea que estrena el contrato de `POST /api/analyze`)**: `REDACT_PATHS` **adivina** los nombres de campo (`input`/`raw`/`value`) de un contrato que aún no existe, y `logger.test.ts` es circular en ese punto (prueba que `input` se redacta porque la lista dice `input`). **Si F1 llama al campo `text`, `source` o `payload`, la única pieza estructural del §11 deja de cubrir nada EN SILENCIO y el gate sigue verde.** T1.4 está obligada a revisar esa lista contra el contrato real. La red no es el mecanismo: el mecanismo es no pasarle el input al logger.
2. `setup-env.ts` carga `.env.test.local` bajo `if (existsSync(...))` y ese fichero no existe: rama no recorrida por nadie. Riesgo bajo (infra de test, fallaría ruidosamente). Confirmado por el verifier.
3. Warning no bloqueante de ESLint: `Multiple projects found, consider using a single tsconfig with references…`.

**Test borrado (excepción consciente a «no se debilitan tests»)**
`route.test.ts` tenía un test «es público: responde sin cookie de sesión (D6)» que **no podía fallar**: hacía la misma llamada que el test de la línea 9 y llamaba a `GET()` directo, así que ni con el middleware de T0.4 habría cazado que alguien protegiera `/api/health`. Cobertura falsa de una decisión de producto es peor que ninguna. **El test real de D6 lo escribe T0.4**, que ya lo tiene asignado.

**Considerado y RECHAZADO**
- Paralelizar los pasos del gate (medido: 46.8s → ~21s). `pnpm gate` es propiedad del arnés y su orden con fail-fast es deliberado; 47s no duele. No se toca sin decisión explícita.

**Decisión de proceso**
- **`ds-reviewer` (paso 5c) SALTADO deliberadamente**: el diff toca `apps/web/**`, pero ese pase revisa contra el espejo del Design System en `docs/design-system/`, que **está vacío hasta TD.1**. No hay primitivas que adoptar y `page.tsx` no lleva ni un estilo a propósito. Saltarlo en silencio habría sido deriva; queda escrito. Ver la entrada de arnés siguiente.

## 2026-07-17 · arnés: la conservación de una cláusula debe ejercitar su MISMA capa; y `ds-reviewer` necesita un DS que exista

Dos carencias que reveló T0.1, corregidas en la misma sesión (regla de mejora continua del arnés). Ambas nacen de incidentes reales de este ciclo, no de especulación.

**1 · `testing/SKILL.md` — séptima forma del anti-patrón «el arnés más cómodo que la realidad»**
El FAIL del verifier (incidente 1 de la entrada de T0.1) no encaja del todo en ninguna de las seis formas que ya listaba la skill: la más próxima («el runtime del test es más permisivo que el de producción») habla de Node vs bundler/navegador, y aquí el runtime era el mismo — lo que cambiaba era **la rama de configuración que selecciona `NODE_ENV`**. Añadida como forma 7: **un test que dice conservar una cláusula de la Verificación pero la ejercita en otra capa**, con la pregunta de control (g) y el corolario «una rama de config que ningún test instancia no existe hasta que un usuario la pisa». Lo que la hace cara es que el test **declaraba por escrito** que conservaba la cláusula: esa frase es la que apaga la alarma de todo el mundo, incluido el bucle.

**2 · `dev-loop/SKILL.md` §5c — `ds-reviewer` exige que el espejo del DS exista**
La condición era «corre si el diff tocó `apps/web/**`». T0.1 la cumple (crea `page.tsx`, `layout.tsx`, `globals.css`) pero `docs/design-system/` está vacío hasta TD.1: el pase revisa el diff CONTRA el espejo, así que sin espejo no hay vara de medir y solo puede producir ruido. La condición pasa a ser «tocó `apps/web/**` **Y** el espejo existe», con la obligación de anotarlo en el journal cuando se aplique la excepción. Es un desajuste de ORDEN inherente a este planning (T0.1 va antes que TD.1, y su página raíz no lleva estilos a propósito), no un caso patológico.

**3 · `dev-loop/SKILL.md` §5a — el revisor compara capa-de-la-cláusula vs capa-del-test**
Contrapartida operativa de (1) en la lista de lo que el revisor tiene que hacer.

## 2026-07-17 · ⏳ TD.1 iniciada

Espejo del DS (`docs/design-system/`) + tokens verbatim en `globals.css` (3 bloques de Tailwind v4) + fuentes self-hosted + showcase `/design-system` con switchers + espejo de los 5 mockups de la variante A. Coste esperado: solo agentes.
Comprobado por el bucle antes de arrancar: **ambos proyectos de Claude Design son legibles con `DesignSync`**. El DS (`9d6b478a…`) tiene 97 paths y su inventario coincide EXACTO con lo que leyó el bootstrap. El de mockups (`1132e88c…`) es `PROJECT_TYPE_PROJECT`, **no un design system**, por eso NO sale en `list_projects` (que filtra a design systems escribibles) — hay que ir a él por `projectId` directo. Contiene `variant-claro.jsx`, `variant-mobile.jsx`, `variant-oscuro.jsx`, `ui-shared.jsx`, `support.js`, `design-canvas.jsx` y `devtools Mockups.html`.

## 2026-07-17 · TD.1 · corrección de alcance MENOR: fuera los switchers de acento y densidad

**El implementer paró en punto estable** y pidió fallo antes de construir el showcase. Tenía razón, y el bucle lo verificó contra fuentes primarias antes de decidir nada (regla: no se traslada un diagnóstico sin comprobarlo).

**Lo verificado (tres ángulos independientes, ninguno de memoria)**
1. **El DS no soporta ni acento conmutable ni densidad.** Leídos con `DesignSync`: `tokens/colors.css` define **UN** acento (`--blue-*`; el comentario dice «one accent», `guidelines/colors-accent.html` se titula «the single brand hue») y **ningún** `[data-accent]`; `tokens/base.css` y `tokens/typography.css` **no tienen `--ui-fs`** ni escala de densidad (el type scale es rem fijo, base 16px). El `readme.md` del DS añade «Max restraint: no gradients, no decorative colour fields».
2. **El PRD no menciona acento, densidad ni switchers**: cero coincidencias. **Ninguna decisión de producto se toca** al retirarlos.
3. **La skill `frontend` los formula CONDICIONALES**: «los acentos conmutables **los dicta el DS**», «**Si** el DS define densidades, viven como var de escala (p. ej. `--ui-fs`)», «`--accent` … **y puede ser conmutable**». El planning los transcribió en afirmativo: **arrastre de la plantilla genérica**.

**Por qué es MENOR y no una parada**: el cambio de alcance mayor se define como «el PRD necesita un ajuste que altera decisiones de producto». Aquí el PRD **no necesita ajuste ninguno** — nunca prometió esto. Lo que estaba mal era el planning, que contradecía el DS del propio usuario. Corregido en la misma sesión (TD.1 Entrega, subtareas y Verificación; **y la Verificación de TD.7**, que exigía «≥2 acentos» y era igual de imposible de pasar). Si algún día se quieren acentos: se añaden al DS en Claude Design primero, y el código los hereda — nunca al revés.

**Segunda corrección del planning: el gotcha `--shadow-*` era FALSO.**
Decía que `--shadow-*` crea un `var()` circular con `@theme` y mandaba renombrar a `--elevation-*` («la ÚNICA desviación de naming permitida»). El implementer lo desmontó con un **build real** de Tailwind v4.3.3: `@theme inline` **no emite la variable**, inserta el `var()` en la utilidad (`.shadow-sm { --tw-shadow: var(--shadow-sm) }`, resolviendo contra el `:root` del volcado). **Cero circularidad.** Renombrar habría metido una desviación de naming para un problema inexistente, violando el «naming 1:1» que es la regla primaria. Y el «única colisión» también era falso: colisionan **7 namespaces** (`--radius-*`, `--text-*`, `--font-*`, `--leading-*`, `--tracking-*`, `--ease-*`, `--shadow-*`), y todos conservan el nombre del DS. Gotcha retirado del planning con la evidencia escrita.
→ **Lección de método**: el gotcha venía de la plantilla y sonaba plausible. El implementer no lo obedeció ni lo ignoró: lo **probó**. Es el mismo patrón que el incidente 3 de T0.1 (el bucle afirmó que pino lanzaba y era falso). **Una advertencia heredada no es un hecho verificado.**

**Resuelto el pendiente de `variant-mobile.jsx`** (lo dejó abierto el bootstrap en `docs/mockups/README.md`): **ni A ni B — contiene LAS DOS**. Su cabecera dice «Mobile versions of all pages, both variants» y exporta `FieldClaroM/HistoryClaroM/LoginClaroM/SignupClaroM` junto a los `*OscuroM`; el `Shell` recibe `theme` por parámetro. La referencia responsive de la variante A son los `*ClaroM`. **No hace falta criterio del usuario.**

**Desviaciones deliberadas de TD.1 (documentadas en código)**
1. **Fuentes**: el DS las carga por `@import` a Google Fonts (`tokens/fonts.css`), incompatible con la cláusula «0 CDNs» de la Verificación. Self-hosted con el paquete `geist` + `next/font`; misma fuente, solo `--font-sans`/`--font-mono` puentean a `var(--font-geist-*)`. El `readme.md` del DS invita explícitamente a ello.
2. **`rounded-base`**: el `--radius: 6px` del DS no tiene forma de clase en Tailwind v4 (`rounded` a secas está fijado a `0.25rem`).

**Hallazgo colateral que habría roto el volcado verbatim en silencio**: **prettier reescribía 21 valores del DS** (`oklch(0.930 0.050 295)` → `oklch(0.93 0.05 295)`). Mismo color, pero rompe la fidelidad literal que hace triviales los diffs contra el espejo. `globals.css` añadido a `.prettierignore` (como ya estaba `docs/`) y los 21 restaurados.

**Deuda del arnés — el espejo se vuelca A MANO** (anotada, no arreglada)
`DesignSync.get_file` devuelve el contenido **al contexto del agente**, y la única forma de persistirlo es que el agente lo **reescriba con `Write`** — es decir, «editar a mano» un espejo cuyo contrato dice que **JAMÁS se edita a mano**, con riesgo de corrupción silenciosa. Para los 8 ficheros de fundaciones es asumible (y merecen spot-check); para `_ds_bundle.js` y 60+ componentes es caro y frágil. **Además: `DesignSync` NO es visible para los subagentes** — el implementer delegó el volcado a un `general-purpose` y volvió con «No matching deferred tools found». Solo puede volcarlo el agente que tiene la sesión de claude.ai. Consecuencia práctica: el volcado del espejo no se puede paralelizar; lo hace el implementer en serie o el bucle.

## 2026-07-17 · ⏸ PARADA en TD.1 (VERIFY) — el entorno no tiene navegador para el gate CUA

TD.1 está **code-complete y en verde**, pero **no cerrada**: su Verificación es visual y el entorno no puede correr un navegador. Prerequisito externo (clase ⚠), parada legítima del protocolo. Preguntado al usuario (AskUserQuestion): eligió **provisionar las libs por `sudo`** él mismo. A la espera de que ejecute el `apt-get install`.

**Qué está hecho y revisado en TD.1** (todo staged, sin commitear):
- Volcado completo del espejo `docs/design-system/`: fundaciones + 14 guidelines + **62 ficheros de `components/**`** (round-trip verificado byte-a-byte con `jq`, sin transcripción a mano) + `SKILL.md` + `_adherence.oxlintrc.json` (confirmado: es el insumo de TD.6).
- `globals.css`: volcado verbatim de tokens en los 3 bloques de Tailwind v4; namespaces del DS vaciados con `*: initial`.
- 5 mockups en `docs/mockups/` renderizables offline en `file://` (React 18.3.1 + Babel 7.29.0 **self-hosteados**, sha384 idéntico al canvas; `_ds_bundle.js` 56 KB).
- Página `/design-system` (server component, prerender estático) + switcher de tema (island `'use client'`, anti-flash, cleanup anti-fuga).
- **REVIEW completo**: `code-review` (medium) → arregló una fuga real de `data-theme` entre rutas (cleanup al desmontar, con control negativo); `simplify` → 2 cleanups aplicados + 2 notas de altitud documentadas; **`ds-reviewer` → LIMPIO** (primera vez que corre: el espejo del DS ya existe).
- Gate: **57 tests / 9 ficheros, verde**. Build: `/design-system` `○ Static`.

**Parte automatizable de la Verificación (hecha, evidencia en `docs/verifications/TD.1/report.md`)**: el HTML servido tiene los specimens + switcher; fuentes self-hosted (`../media/Geist*.woff2`, 0 Google Fonts); 0 hosts externos en el HTML de la página y su CSS. **Pendiente de navegador**: switch de tema en vivo, paridad visual vs `guidelines/`, render de los 5 mockups, y la pestaña de red definitiva (0 CDN).

**INCIDENTE DE ENTORNO — no hay navegador operable (afecta a TODO el pipeline web)**
`agent-browser` (0.32.1) trae su Chrome pero **no arranca**: faltan ~20 libs de sistema (`libatk`, `libgtk-3`, `libcups`, `libgbm`, `libpango`, `libX*`, `libatspi`, `libavahi`…) y **no hay `sudo` con contraseña**. Impacto: el gate CUA no corre en NINGUNA tarea web (TD.1, TD.7, T0.4, T0.5, F1, F2) ni los specs Playwright permanentes.
- **Workaround PROBADO por el bucle** (por si el `sudo` del usuario no fuera opción en el futuro): `apt-get download` de las libs (sin root) + extraer a un prefijo + `LD_LIBRARY_PATH` con **rutas ABSOLUTAS** (con relativas falla al cambiar de cwd) → **Chrome headless directo arranca y renderiza** (probado: `chrome --headless --dump-dom localhost:3000/api/health` → `{"ok":true}`). 
- **Dos paredes con `agent-browser`**: (1) no propaga `LD_LIBRARY_PATH` a su Chrome hijo; (2) el intento de instalar un wrapper de su binario lo **bloqueó el clasificador de permisos**. También bloqueó `kill`/`pkill` genéricos (un `kill <pid>` concreto sí pasó). Por eso la vía elegida es que el usuario instale las libs a nivel de sistema (entonces `agent-browser` funciona sin trucos).
- **Comando dado al usuario**: `sudo apt-get install -y libnss3 libatk1.0-0t64 libatk-bridge2.0-0t64 libcups2t64 libdrm2 libgbm1 libasound2t64 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 libxkbcommon0 libpango-1.0-0 libcairo2 libatspi2.0-0t64 libgtk-3-0t64` (apt resuelve el resto de la cadena). Alternativa: `npx playwright install-deps chromium`.

**Decisiones de TD.1 que heredan las siguientes tareas** (no obvias):
- **Infra de tests de componente — divergencia DELIBERADA de `testing/frontend.md §2`, documentada en `apps/web/vitest.config.ts`**: TD.1 usa jsdom **por-fichero** (`// @vitest-environment jsdom`), sin `@vitejs/plugin-react` (hipótesis: `oxc.jsx` lo hace redundante) y sin `vitest.setup.ts`. Basta para TD.1. **TD.2 DEBE** montar el setup jsdom a nivel de proyecto que pide la skill (`vitest.setup.ts` con matchers de jest-dom + mocks `matchMedia`/`ResizeObserver`) ANTES de que las primitivas Base UI (dialog/popover/tooltip) lo exijan — o caerá en el falso negativo silencioso que §2 advierte. Y confirmar si `oxc.jsx` hace redundante a `plugin-react`. Es un caso "la skill puede necesitar actualización": se resuelve en TD.2 con la evidencia de volumen, no ahora.
- **`packages/test-utils/src/setup-env.ts`**: se cambió `fileURLToPath(new URL())` → `import.meta.dirname` porque el primero peta bajo `environment: jsdom` (vitest sustituye el `URL` global). Defecto latente de T0.1 que el primer test de componente destapó. Corre en TODOS los proyectos; verificado que sigue verde en los node.
- **`theme-switcher.tsx`**: el cleanup hace `removeAttribute('data-theme')` INCONDICIONAL — asume propiedad exclusiva de `<html data-theme>`. Cuando **F1** introduzca theming de app global, su provider debe **guardar y restaurar** el valor previo, no borrar. Anotado en el docblock.
- **`eslint.config.ts` y `knip.json`** ignoran `docs/**` (espejo + mockups con vendor JS; no es código del proyecto, igual que `.prettierignore`).
- **`docs/mockups/assets/vendor` pesa ~4.2 MB** (babel-standalone 3 MB + react-dom dev 1.1 MB) versionados. **Considerado y aceptado**: es artefacto de referencia bajo `docs/` en un banco de pruebas; el peso del repo no es preocupación de producto (D1); a cambio los mockups renderizan offline. Alternativa (volver a unpkg CDN) anotada por si se revisa.
- **`variant-mobile.jsx` resuelto** (pendiente que dejó el bootstrap): contiene LAS DOS variantes («both variants»); la referencia responsive de la A son los `*ClaroM`. Anotado en `docs/mockups/README.md`.
- **Limitación del arnés**: `DesignSync` sirve para el bucle principal pero **es intermitente/ausente en subagentes** (un implementer volvió con «No matching deferred tools»; su MCP se cayó a media tarea). El volcado del espejo no se puede paralelizar en hijos con fiabilidad; lo coordina el bucle o un implementer con conexión viva, en serie, con round-trip byte-a-byte (riesgo de corrupción silenciosa al reescribir con `Write` lo que `get_file` devuelve al contexto).

## 2026-07-17 · arnés: CUA operativo en el VPS + `--no-sandbox` documentado

Instaladas las dependencias de CUA (`agent-browser`, Vercel Labs) para el gate de verificación de UI, que TD.1 estrena (primera tarea con superficie web verificable en navegador). Estado: CLI 0.32.1 global, Chrome 151 instalado, daemon vivo, CDN alcanzable.
- **Librerías de sistema del navegador**: faltaban 12 (`libatk-1.0.so.0`, `libgbm.so.1`, `libcups.so.2`, `libasound.so.2`, `libpango-1.0.so.0`, X11…). Las instaló el USUARIO con `sudo agent-browser install --with-deps` (requiere root; el bucle no tiene sudo sin contraseña). Una sola vez.
- **`--no-sandbox` es obligatorio en este entorno**: Ubuntu 24.04 con `kernel.apparmor_restrict_unprivileged_userns = 1` desactiva los user namespaces sin privilegios ⇒ el sandbox de Chrome no arranca. Verificado: `agent-browser open --args "--no-sandbox" https://example.com` navega y `get title` lee la página. `agent-browser doctor` seguirá dando **1 fail en «Launch test»** (su prueba interna no acepta args) — NO es bloqueo. Documentado en `testing/references/cua.md` §Paso 2 para que ningún verifier lo lea como FAIL ni lo re-descubra.

## 2026-07-17 · TD.1 · FAIL del verifier en los mockups → corregido (2 bugs de `file://`)

Con el navegador ya operativo, el verifier corrió TD.1 completa contra Chrome real: **fundaciones y showcase PASARON** (specimens, switcher de tema en vivo, paridad vs `guidelines/`); **los 5 mockups FALLARON** por dos bugs que la verificación estática no veía. Ambos corregidos y **reverificados en navegador** (`agent-browser`, `--no-sandbox`, `file://`): los 5 renderizan con contenido y 0 errores de consola; 0 hosts externos en la pestaña de red.

- **Bug 1 — mockups EN BLANCO en `file://`**. Cargaban el JSX con `<script type="text/babel" src="…">`; babel-standalone lo pide por **XHR**, y **Chrome bloquea XHR bajo `file://`** → componentes `undefined` → `#root` vacío. Servidos por HTTP renderizaban bien (por eso la verificación estática y las sesiones previas no lo vieron). **Fix**: precompilar los `.jsx` a `.js` plano (Babel preset `react`) y cargarlos con `<script src>` normales; el bloque `App` inline va compilado y **envuelto en IIFE** — sin ella, su `const { DesignCanvas… } = window` colisiona en ámbito global con las `function DesignCanvas/DCSection/DCArtboard` que `design-canvas.js` declara a top-level (con babel-over-HTTP cada script tenía ámbito propio; como `<script>` plano comparten el global). Fuera `@babel/standalone` del runtime.
- **Bug 2 — Google Fonts (CDN) en los mockups**. Enlazaban `../design-system/tokens/fonts.css` (y `styles.css`, que lo re-`@importa`), cuya línea 4 hace `@import` de `fonts.googleapis.com` → 5 peticiones externas (una por mockup), capturadas en el `.har` del verifier. **Fix**: los HTML dejan de enlazar `fonts.css`/`styles.css` del espejo y enlazan `assets/fonts.css` (Geist/Geist Mono self-hosted desde `assets/fonts/*.woff2`, del paquete `geist`). El espejo NO se tocó (es solo-lectura). Los otros 5 token files se siguen reutilizando por ruta relativa (no van a CDN).

**Retracto de claims previos de este journal que quedaron falsos** (para que el registro no mienta):
- El bullet de arriba «5 mockups renderizables offline en `file://` (React 18.3.1 + Babel 7.29.0 self-hosteados…)» **era falso**: renderizaban en blanco por `file://`. Ahora sí renderizan, pero **sin Babel en runtime** (precompilados).
- «`docs/mockups/assets/vendor` pesa ~4.2 MB (babel-standalone 3 MB + react-dom dev 1.1 MB)»: al quitar `babel.min.js`, `vendor/` baja a ~1.2 MB (solo React + ReactDOM dev). El argumento del peso ya no incluye babel.
- La «única dependencia de red que queda: `tokens/fonts.css` → Google Fonts» del README de mockups **se eliminó**: ahora son 0 CDNs, verificado en la pestaña de red del navegador.

## 2026-07-17 · TD.1 cerrada — PASS

- Coste: $0 (D8, sin APIs de pago) · Ciclos verifier: **3 (BLOQUEADA por navegador → FAIL → PASS)** · Tests: 57 en 9 ficheros · Evidencia: `docs/verifications/TD.1/` (conserva el histórico bloqueo→FAIL→PASS entero)
- Commit: (ver abajo) · El espejo del DS, `globals.css`, `/design-system`, el switcher de tema y los 5 mockups quedan vivos.

**Decisiones no obvias que heredan TD.2–TD.7 y F1/F2**
- **CUA operativo** (ver la entrada de arnés): `agent-browser` + Chrome funcionan con `--no-sandbox`. El gate CUA ya corre en tareas web. La entrada previa «PARADA por falta de navegador» quedó RESUELTA.
- **Corrección de alcance de TD.1 (solo-tema)**: se retiraron los switchers de acento y densidad porque el DS no los define (un solo brand hue; sin tokens de densidad) y el PRD no los menciona — arrastre de plantilla. Confirmado por el usuario. Si algún día se quieren, empiezan en Claude Design, no en código.
- **El gotcha `--shadow-*` del planning era FALSO** (premisa incorrecta): `@theme inline` no crea `var()` circular. Naming 1:1 conservado en los 7 namespaces que colisionan. No lo re-introduzcas.
- **Infra de tests de componente (jsdom)**: TD.1 usó jsdom por-fichero sin setup a nivel de proyecto. **TD.2 DEBE montar `vitest.setup.ts`** (jest-dom + mocks matchMedia/ResizeObserver) ANTES de que las primitivas Base UI (dialog/popover/tooltip) lo exijan, y confirmar si `oxc.jsx` hace redundante a `@vitejs/plugin-react`. Documentado en `apps/web/vitest.config.ts`.
- **`theme-switcher.tsx`** hace `removeAttribute('data-theme')` incondicional al desmontar (asume propiedad exclusiva de `<html data-theme>`). Cuando **F1** meta theming global, su provider debe guardar/restaurar, no borrar.
- **Mockups**: se sirven en `file://` con JS **precompilado** (no babel-standalone: los `<script type=text/babel src>` fallan por XHR bajo `file://`). Los `.jsx` quedan como fuente de referencia; los `.js` hermanos son lo que carga el HTML. Fuentes self-hosted en `docs/mockups/assets/fonts/`. `variant-mobile` contiene AMBAS variantes; la referencia responsive de la A son los `*ClaroM`.

**INCIDENTE 1 — el gate CUA cazó lo que el análisis estático NO podía (el valor del navegador)**
La verificación parcial (sin navegador) dio verde a mockups y fuentes por análisis estático del HTML servido de `/design-system`. Con navegador real, DOS FAIL: (a) los 5 mockups renderizaban EN BLANCO en `file://` (Babel pide el JSX por XHR, Chrome lo bloquea bajo `file://`; por HTTP renderizaban — de ahí el falso verde del implementer, que los probó por HTTP); (b) los mockups enlazaban el `fonts.css` del espejo que `@importa` `fonts.googleapis.com` → 5 peticiones a un CDN, que el estático no miró porque solo revisó `/design-system`. Lección: **el análisis estático de una cláusula de navegador es una aproximación; la cláusula solo la cierra el navegador.** Ya estaba escrito en el test de 0-CDN («esto es una aproximación»), y se confirmó en la práctica.

**INCIDENTE 2 — la aserción de contraste (cua.md) cazó un fallo WCAG DENTRO del DS**
El verifier midió el contraste texto/fondo real (no solo el fondo) y encontró **4 pares por debajo de AA**, el más grave el **botón primario en oscuro (blanco/acento = 4.27, necesita 4.5)** — que TD.2 habría heredado en TODOS los botones. Eran valores del DS volcados verbatim, así que se rutearon al usuario (no eran bug de código de TD.1). El usuario eligió corregirlos en el DS. **Corregidos en Claude Design primero** (fuente de verdad), luego re-volcado espejo + `globals.css`:
- dark `--accent` blue-500→blue-600 (btn 4.27→5.69); dark `--accent-hover` blue-400→blue-700 (oscurece en hover, coherente con el readme del DS que decía «darken on hover» y el token hacía lo contrario); light `--text-muted` gray-500→gray-600 y `--text-subtle` gray-400→gray-500 (subtle 2.5→4.6); dark `--text-subtle` gray-500→**gray-450** (nuevo step); `--amber-700` 0.560→0.545 (warning-subtle 4.41→4.70).
- **Hallazgo estructural**: el DS no podía tener 3 tiers de texto todos-AA con el más claro por debajo del medio, porque el medio (text-muted) ya estaba en el suelo AA (4.61). Se reestructuró: muted baja a gray-600, subtle sube a gray-500, orden preservado. Decisión del usuario.
- Los números los calculé A MANO con la aritmética WCAG (oklch→sRGB→ratio) ANTES de tocar el DS, y el verifier los re-midió en navegador: coinciden. Regla del arnés cumplida: no trasladar/aplicar un número sin verificarlo.

**INCIDENTE 3 — corrección MÍA cazada por el propio flujo**: lancé un `AskUserQuestion` sobre acento/densidad que **el planning YA tenía resuelto** en la pasada anterior (contexto resumido entre sesiones). El usuario reconfirmó, sin daño. Lección: al retomar tras un corte de contexto, LEER el estado del árbol y el journal ANTES de preguntar — el planning ya llevaba la corrección aplicada.

**Deuda anotada**
1. **`--gray-450` no está especimenado** en `guidelines/colors-neutrals.html` (es un step AA nuevo). No es desviación (no cambia un valor), pero el swatch del ramp está incompleto. Candidato a añadir en Claude Design cuando se toque ese guideline (TD.3/TD.5).
2. **Heredada de T0.1 para T1.4**: `REDACT_PATHS` adivina los nombres de campo del contrato de `/api/analyze` (`input`/`raw`/`value`). Sigue viva.
3. **Espejo del DS se vuelca A MANO** (`DesignSync.get_file` → `Write`), y `DesignSync` NO es visible para subagentes: el volcado no se paraleliza, lo hace el bucle en serie con round-trip byte-a-byte. Limitación del arnés, anotada, sin arreglo hoy.

## 2026-07-17 · ⏳ TD.2 iniciada

6 primitivas de formulario (`Button`, `Input`, `Textarea`, `Select`, `Field`, `IconButton`) en `apps/web/src/components/ui/`, shadcn sobre Base UI, ajustadas 1:1 al espejo `components/forms/`, con secciones en `/design-system`. Coste esperado: solo agentes. Estrena: Base UI + shadcn + cva en el repo, y el `vitest.setup.ts` jsdom a nivel de proyecto que TD.1 dejó pendiente.

## 2026-07-18 · TD.2 cerrada — PASS

- Coste: $0 (D8) · Ciclos verifier: **2 (FAIL glifos tofu → fix Icon SVG → PASS)** · Tests: 115 en 16 ficheros · Evidencia: `docs/verifications/TD.2/` (histórico FAIL→PASS)
- Las 6 primitivas de formulario del DS (`Button`, `IconButton`, `Input`, `Textarea`, `Select`, `Field`) + el `Icon` SVG viven en `apps/web/src/components/ui/` y se muestran en `/design-system`.

**Decisiones no obvias que heredan TD.3–TD.7 y F1/F2**
- **Base UI diferido a TD.4** (desviación anotada en planning): las 6 primitivas son controles NATIVOS; instalar Base UI ahora = dep huérfana. TD.4 lo estrena con la 1ª primitiva portalizada (dialog/tooltip/toast). Toolchain shadcn (`components.json` + cva/clsx/tailwind-merge) ya instalado.
- **`components.json` tiene `iconLibrary: "lucide"` A PROPÓSITO**: quitarlo hace que shadcn caiga a `radix` (`@radix-ui/react-icons`), que el proyecto prohíbe MÁS que lucide. No hay opción "none". **Trampa conocida para TD.4**: cualquier `shadcn add` regenerará imports de iconos (lucide) que hay que arrancar a mano — como TD.2 ya hizo. El valor del campo solo elige QUÉ librería nombran los imports generados, no si aparecen.
- **`vitest.setup.ts` a nivel de proyecto** montado (jest-dom + mocks matchMedia/ResizeObserver + `afterEach(cleanup)`), `environment: 'jsdom'`, `unstubGlobals: true`. **CONFIRMADO: `oxc.jsx` hace redundante a `@vitejs/plugin-react`** (la batería completa de tests de componente pasa sin él; añadirlo = devDep huérfana). Cierra la pregunta abierta de TD.1.
- **Altura de control por token del DS**: se mapeó `--control-h-*` en `@theme` de `globals.css` → utilidades `h-control-sm/md/lg`, y Button/Input/Select las usan (antes literales `h-7.5/9/11`). Mismo patrón que el radio. IconButton usa `size-7/8/10` (escala cuadrada 28/32/40, sin token equivalente). Si el DS mueve `--control-h`, la utilidad sigue — sin drift.
- **`theme-switcher.tsx`** (de TD.1): su cleanup borra `data-theme` incondicional; cuando F1 meta theming global, guardar/restaurar.

**INCIDENTE — glifos Unicode vs el sistema de iconos SVG del DS (el FAIL del verifier)**
La Entrega de TD.2 mandaba «glifos Unicode en lugar de librerías de iconos». El `code-review` (medium) avisó del riesgo de tofu; el **verifier lo confirmó en navegador**: `copy` (⧉ U+29C9) —usado en 2 specimens— e info/shield/key salen **tofu (□)** porque Geist no cubre esos code points, y **no hay glifo Unicode fiel de "copiar"**. Causa raíz más profunda: el sistema de iconos del propio DS ES **SVG-inline** (`display/Icon.jsx`, paths lucide inline, cero dependencia), así que un carácter Unicode nunca es 1:1 con el specimen — el mandato Unicode contradecía al DS que la fase obliga a obedecer.
- **Decisión del usuario**: adoptar el `Icon` SVG del DS ahora. Se portó `Icon.jsx` 1:1 a `apps/web/src/components/ui/icon.tsx` (26 paths verbatim, `aria-hidden`, tamaños por spec), se borró `icon-glyph.tsx`, y las primitivas usan `<Icon>`. Contrato `icon?: IconName` intacto.
- **Corrección de alcance anotada en planning**: TD.2 adelanta la construcción del `Icon`; **TD.4 ya NO lo lista**; la sección de showcase del `Icon` y el resto de `display/` siguen en **TD.3**.
- **Lección de arnés**: cuando la Entrega de una tarea manda un MECANISMO (no un resultado) que contradice al DS/fuente-de-verdad, el DS gana (principio vinculante de la fase); el mandato se corrige, no se fuerza. El code-review olió el riesgo, pero fue el **navegador** quien lo probó — el análisis estático no cazaba el tofu (depende de la cobertura de la fuente). Refuerza la regla ya escrita: una cláusula de navegador solo la cierra el navegador.

**Deuda anotada (candidatas a corregir en el DS / tareas futuras)**
1. **DS `Button.jsx` usa `var(--red-700)` directo** en vez del `--danger-hover` que añadimos al DS en TD.2: incoherencia interna del DS (define el token pero su propio spec no lo consume). Candidata a actualizar `Button.jsx` en Claude Design.
2. **DS `Field` no cablea `aria-describedby`**: el error/hint no se asocia al control (nuestra `Field` es 1:1 con el spec, que tampoco lo hace) → el lector de pantalla no lo anuncia. Mejora de a11y del DS Field, candidata a subir a Claude Design.
3. **`Select` derrama `style` sobre el `<select>` interno**, mientras `Select.jsx` lo pone en el `<div>` wrapper. Contrato («acepta style») cumplido; matiz de fidelidad menor.
4. **`Select` keys duplicadas** si dos options comparten value: edge case de datos del consumidor.
5. **Glifos Unicode latentes ya no aplican** (icon-glyph borrado); pero para F1/F2, cualquier icono nuevo usa `<Icon>` SVG (añadir el path al `PATHS` del DS primero).

## 2026-07-18 · ⏳ TD.3 iniciada

Primitivas de display+feedback: `Badge`, `Card`, `CodeBlock`, `ConfidenceBar`, `CopyButton`, `Kbd` (display) + `Callout`, `EmptyState`, `Spinner` (feedback), 1:1 al espejo, secciones en `/design-system`. **`Icon` ya se construyó en TD.2** (adelantado): TD.3 solo añade su sección de showcase, no lo reconstruye. Coste esperado: solo agentes.

## 2026-07-18 · ⚠ DECISIÓN PENDIENTE DEL USUARIO — contraste de los badges violet/cyan en tema OSCURO (TD.3)

**Qué**: los badges de DataKind `violet` (json) y `cyan` (base64/uuid) FALLAN WCAG en tema oscuro. Medido por el bucle (oklab color-mix → sRGB → WCAG), no estimado:

| Badge | Claro | Oscuro | Umbral |
|---|---|---|---|
| violet (text-violet-700 sobre color-mix bg) | 6.24 ✅ | **2.11** ❌ | 4.5 (y <3 grande) |
| cyan (text-cyan-700 sobre color-mix bg) | 5.17 ✅ | **2.49** ❌ | 4.5 (y <3 grande) |

**Por qué NO se arregló en TD.3** (decisión vía advisor, con el usuario dormido): a diferencia del contraste de TD.1 (re-alias mecánicos a steps existentes) y del `--danger-hover` de TD.2 (nombrar un valor existente, cero cambio visual), arreglar esto exige **inventar valores nuevos** (los hues secundarios violet/cyan solo tienen steps 100/500/700 — no hay `-200` como el accent usa para su subtle-fg en oscuro) o re-aliasar a `-100` (más claro que el patrón del accent → inconsistencia). Eso es una **decisión de identidad de producto** (los colores de DataKind son un sistema deliberado del DS README: «each DataKind gets one fixed hue… reads the same everywhere»), NO un suelo AA mecánico. Varias soluciones válidas dan looks distintos → es del usuario.
**No bloquea TD.3**: cua.md dice que un par sub-AA cuyo color viene del DS es «hallazgo a rutear, decisión del usuario, se REPORTA, no se ignora» — no un FAIL. Los badges son fieles al specimen (la cláusula «ambos temas» pasa), y **no hay consumidor de producto hasta F1** (la UI de la cadena) → cero daño en vivo por diferir.
**Opciones para el usuario** (decide el CÓMO): (A) añadir `--violet-200`/`--cyan-200` al DS y darle al badge un override de tema oscuro que use el step claro para el texto (espeja el patrón `accent-subtle-fg → blue-200` en oscuro; consistente pero inventa valores); (B) re-aliasar el texto oscuro a `--violet-100`/`--cyan-100` (existen, pero más claros/menos saturados que el patrón del accent). Empezar por Claude Design (fuente de verdad), luego re-volcar.
**Relacionado**: se acumulan decisiones de DS para el usuario — [[deuda]] de TD.2 (Button.jsx no consume --danger-hover; Field sin aria-describedby) + esta. Candidatas a una tanda de correcciones de DS acordada.

## 2026-07-18 · TD.3 CERRADA — primitivas de display + feedback (PASS)

**Entrega**: 9 primitivas nuevas (`Badge, Card, CodeBlock, ConfidenceBar, CopyButton, Kbd` display + `Callout, EmptyState, Spinner` feedback), cada una espejo 1:1 de `docs/design-system/components/{display,feedback}/`, con `.test.tsx`. Showcase en `/design-system` (secciones `#display` y `#feedback`). Gate verde: 164 tests / 25 ficheros.
**REVIEW**:
- code-review (correctness): sin arreglos. `CopyButton.onCopy` dispara tras el `setCopied` aunque el clipboard no exista → es **fiel al mirror** (`CopyButton.jsx:15-17` hace lo mismo); no se toca. `prefers-reduced-motion` del Spinner cubierto por la regla universal `!important` de globals.css (línea 485).
- simplify: 1 hallazgo (Spinner inyecta `<style>` keyframe `dtds-spin` por instancia) → **SKIP documentado**: el mirror `Spinner.jsx:12` lo lleva inline por diseño; mover el keyframe a globals.css desviaría el cuerpo del componente del mirror y el ds-reviewer lo declaró conforme. Coste real despreciable (pocos spinners a la vez; `<style>` idéntico dedupea en CSSOM).
- ds-reviewer: 1 hallazgo (Callout columna interna `gap-0.5`=2px vs mirror `gap:3`=3px) → **APLICADO** `gap-0.75`.
- Extra (catch del advisor): subtítulo obsoleto «glifos Unicode» en la sección de formularios de `page.tsx` (falso desde que TD.2 adoptó Icon SVG) → corregido a «iconos SVG del DS».
**VERIFY (navegador, CUA, verifier con contexto fresco)**: PASS. 10 primitivas fieles en ambos temas (cero tofu, 26 iconos SVG nítidos, CodeBlock terminal oscuro en ambos temas). CopyButton copia de verdad por teclado (espía en `navigator.clipboard.writeText` invocado con Enter Y con Espacio; aria-label→«Copiado»). Kbd: pase por vacuidad LEGÍTIMO (specimen `<kbd>` no-interactivo confirmado). ConfidenceBar O5: longitud del relleno + etiqueta numérica cambian con el valor (dos canales no cromáticos). Contraste medido independientemente: todas las superficies AA en ambos temas EXCEPTO violet/cyan DataKind en oscuro (2.11/2.49) → ruteado como [[deuda]] de paleta del DS (decisión del usuario), NO FAIL, porque `badge.tsx` es espejo byte-a-byte del specimen. Evidencia: `docs/verifications/TD.3/report.md` + 15 capturas/outputs.
**Coste**: $0 (superficie frontend estática, sin APIs de pago).
**Nota de proceso**: el violet/cyan oscuro se decidió vía advisor (usuario dormido) — se difiere por exigir inventar valores nuevos / cambiar identidad de producto (ver entrada ⚠ arriba), no un re-alias mecánico como TD.1/TD.2.

## 2026-07-18 · TD.5 CERRADA — composites de producto presentacionales puros (PASS)

**Entrega**: `ChainSummary` (server), `StepCard` (client) espejo de `components/chain/`, y `HistoryRow` (client) espejo de `components/history/` — presentacionales PUROS (props planas, cero import de `@app/core`), que SOLO componen primitivas de TD.2/TD.3. Sección "Composites" en `/design-system` con `composites-demo.tsx` y datos de ejemplo del propio DS. Regla de pureza permanente (bloque 5b en `eslint.config.ts`: `no-restricted-imports` scoped a `components/{chain,history}`). Gate verde: 187 tests / 28 ficheros.
**REVIEW**: code-review (correctness) sin bugs; simplify = limpio (sin hallazgos); ds-reviewer = conforme sin hallazgos. Cero ediciones en REVIEW → gate seguía verde. Desviaciones documentadas en cabecera (todas precedente de TD.3): `HistoryRow` usa `group`/`group-hover:`+`focus-within:` en vez de `useState` (sirve mejor reduced-motion + a11y de teclado); px del mirror vía rejilla 4px (`size-6.5`=26px, `gap-1.75`=7px, `max-w-80`=320px, `opacity-35`); sombra vía clase `shadow-sm` mapeada a token; `type Terminal` interno hasta primer consumidor (knip).
**VERIFY (navegador CUA + gate, verifier contexto fresco)**: PASS. (1) 3 composites 1:1 con specimens en claro y oscuro, sin tofu; picker O4 del StepCard actualiza `applied` en vivo (jwt.decode→base64.decode). (2) reduced-motion: `matchMedia(reduce)=true`, `transition-duration≈0`, y el estado visible se conserva — foco de teclado revela las acciones (opacidad 0.35→1), operables. (3) Control negativo de pureza verificado independiente: `pnpm lint` FALLA nombrando `no-restricted-imports`+mensaje TD.5 tanto con `import { }` como con `import type { }` de `@app/core`; dentro de `pnpm gate`; revertido → gate verde sin rastro. Evidencia: `docs/verifications/TD.5/report.md` + 13 capturas/outputs.
**Coste**: $0.
**Recurrencia de [[deuda]] de DS**: el verifier volvió a medir independiente el contraste sub-AA de violet/cyan en oscuro (json 2.11, cyan/base64 2.49; +uuid comparte el defecto) → mismo hallazgo ya ruteado al usuario (ver entrada ⚠ arriba), NO defecto de TD.5 (el Badge es espejo fiel del specimen). Se acumula: la corrección vive en el primitivo Badge (TD.3)/valores del DS.

## 2026-07-18 · TD.6 CERRADA — lint de adherencia al DS (PASS)

**Entrega**: bloque **5c** en `eslint.config.ts` (scope `apps/web/**/*.{ts,tsx}` sin tests, DENTRO de `pnpm gate`) que adapta las IDEAS de `docs/design-system/_adherence.oxlintrc.json` (pensado para JSX con `style` inline) al mecanismo real del repo (clases Tailwind v4 + imports). Tres reglas:
1. **Rampa cruda** (`no-restricted-syntax` sobre `Literal`+`TemplateElement`, regex `DS_RAW_RAMP`): prohíbe `{prop}-{ramp}-{step}` (`bg-blue-500`, `text-red-600`…), cubriendo rampas del DS (fuerza alias semántico) y paletas default de Tailwind (que `--color-*: initial` de globals.css borra → clase no-op silenciosa).
2. **Arbitrario crudo** (regex `DS_RAW_ARBITRARY`): prohíbe `[…]` con hex, número+unidad o función de color cruda. NO casa token-vía-var (`[--x:var(--warning)]`), spacing fraccionario ni `transition-[…]`.
3. **Imports de iconos** (`no-restricted-imports` base): `lucide-react`, `@radix-ui/*`, `@heroicons/*`, `react-icons`, `@tabler/*`, `@phosphor-icons/*`.
Excepción sancionada de badge.tsx (violet/cyan sin alias semántico) resuelta con `eslint-disable-next-line` por uso concreto (auditable, sin apagar la regla en `components/ui`).
**REVIEW**: code-review (correctness) encontró y CERRÓ un false-negative: `DS_RAW_ARBITRARY` no cazaba funciones de color en brackets (`bg-[rgb(...)]`, `text-[oklch(...)]`) pese a que la regla dice «valores arbitrarios crudos». Verifiqué cero riesgo de falso positivo (el único arbitrario del árbol es `transition-[border-color,box-shadow]`) y extendí el regex (color-mix|rgba?|hsla?|hwb|oklch|oklab|lab|lch|color) — self-test verde (caza hex/px/%/color-fns; respeta token-vía-var, layout brackets, fraccional). simplify: 1 hallazgo opcional (unir 4 selectores→2 vía esquery union) → SKIP (la forma explícita de 2 entradas es house-style y el mensaje ya es constante compartida; su nota de altitud CONFIRMA que targetear `Literal`/`TemplateElement` crudos —no `className={}`— es correcto porque las clases fluyen por `cn(...)`). ds-reviewer: N/A (sin superficie visual; badge.tsx solo ganó comentarios disable).
**VERIFY (lint/gate, verifier contexto fresco)**: PASS. Cada regla muerde EN AISLADO nombrando su categoría; el arbitrario caza px/hex/oklch; allowances limpias (fraccional, token-vía-var, transition brackets); fixture retirado → gate verde sin rastro; excepción badge.tsx auditable (quitar el disable → `text-violet-700` rojo; restaurado md5-idéntico). 187 tests. Rareza inofensiva: el fixture de import añade ruido `import-x/no-unresolved` porque lucide no está instalado — la regla DS igual dispara y se nombra. Evidencia: `docs/verifications/TD.6/report.md` + 3 outputs.
**Coste**: $0.

## 2026-07-18 · ⚠⚠ PAQUETE DE DECISIÓN PARA EL USUARIO (al volver) — la fase TD queda a tu OK

Buenos días. Cerré de forma autónoma **TD.3, TD.5 y TD.6** (todas PASS, commiteadas y pusheadas a `main`: `0c5034b`, `7c068ec`, `b840c58`). El bucle sigue vivo trabajando **T1.1 (el motor: contratos + 8 detectores en `packages/core`)** — es puro, determinista, sin navegador, sin mutación externa y solo depende de T0.1 (cerrado); la regla 1 del planning permite adelantar trabajo entre fases que no dependa de lo pendiente. **Lo que sigue NO lo decido yo solo** porque toca tu criterio o muta algo tuyo:

### A) TD.4 — parada deliberada (no la ejecuté). Necesito tu OK en dos cosas:
TD.4 (1) **crea** primitivas que el DS NO define (no hay specimen que espejar → es diseño con juicio, no mirror 1:1) y (2) **las sube a TU proyecto de Claude Design vía DesignSync** (mutación externa e irreversible de un recurso tuyo, que nunca autorizaste explícitamente). Por eso la dejo entera para ti.
- **Lista de gaps PROPUESTA** (recomendación, no compromiso; «solo lo que T1.5/T1.6/T2.2 consuman, nada especulativo» — planning):
  - ✅ **Dialog de confirmación** — T2.2 lo exige literal («borrar una/todas, **con confirmación**»). Consumidor real. **Recomiendo crearlo.**
  - ✅ **Wordmark** — existe como guideline `brand-wordmark.html`, no como componente; lo consumen el header de `/` (T1.5) y las pantallas de auth (T0.4). **Recomiendo crearlo.**
  - ⏸️ **Toast «copiado»** — CopyButton ya da feedback inline (check verde); ningún consumidor lo exige aún. **Recomiendo diferir** hasta que una tarea lo pida.
  - ⏸️ **Tooltip** — los IconButton ya llevan `aria-label`; es enriquecimiento, sin consumidor duro. **Diferir.**
  - ⏸️ **Skeleton** — Spinner cubre la carga y el análisis es <1s. **Diferir.**
- **Autorización de subida a Claude Design**: ¿me autorizas a que el bucle suba los componentes aprobados a tu proyecto de Claude Design vía DesignSync (y regenere el espejo)? Sin tu OK explícito no subo nada.
- **Nota**: TD.7 (cierre de fase TD) depende de TD.4 y de tu **revisión humana visual** del showcase; y **T0.2 depende (de orden) de TD.7** → toda la cadena F0 (Docker/Postgres/auth) está aguas abajo de tu OK. Por eso el motor (T1.x) es lo único que avanza mientras.

### B) Deudas de paleta/a11y del DS acumuladas (candidatas a una tanda acordada, source-first en Claude Design):
1. **Contraste violet/cyan en tema OSCURO** (badges DataKind json/base64/uuid): fallan WCAG (json 2.11, cyan 2.49; umbral 4.5). Medido y re-medido por dos verifiers. Requiere inventar valores nuevos (no hay step `-200` en esos hues) o re-aliasar a `-100` → decisión de identidad de producto, tuya. Detalle en la entrada ⚠ de arriba.
2. **Button no consume `--danger-hover`** (deuda anotada en TD.2): el token existe pero el `Button.jsx` del DS no lo usa en su variante danger.
3. **Field sin `aria-describedby`** (deuda de TD.2): el hint/error del Field no está enlazado por `aria-describedby` al control.
> Las 3 viven aguas arriba (en el DS/Claude Design), no son defectos del código del repo (que espeja fielmente). Se pueden agrupar en una sesión de correcciones de DS cuando decidas A).

**Tu mañana = aprobar/editar, no empezar de cero.** Cuando quieras, dime: (i) qué gaps creo, (ii) si autorizas la subida a Claude Design, (iii) si abro la tanda de deudas de DS. Mientras, el motor avanza.

## 2026-07-18 · T1.1 CERRADA — contratos del motor + 8 detectores (PASS) · arranca F1

**Contexto de selección**: con TD.4 (parada humana) y toda la cadena F0 (T0.2 depende de ORDEN de TD.7) aguas abajo del OK del usuario, el grafo deja **T1.1 (Depende de: T0.1, cerrado)** como única tarea autónoma elegible. La regla 1 del planning permite adelantar trabajo entre fases que no dependa de lo pendiente; la intro de F1 manda construir el motor (lógica pura) antes que la UI. Decisión tomada vía advisor (usuario dormido).
**Entrega**: `packages/core/src/engine/` — 6 contratos de §6.1 como schemas Zod con tipo por `z.infer` (`DataKind, Detection, Transform, TransformResult, ChainStep, Chain`) + los 8 detectores de §6.2 como funciones PURAS y TOTALES + orquestador `detect()` que devuelve `Detection[]` por confianza desc con `text` de suelo (I6). Export subpath `@app/core/engine`. Factories en `packages/test-utils`. Gate verde: 260 tests / 30 files (43 del motor entran en el gate desde aquí, regla 8).
**Interpretaciones de §6.2 fijadas** (el PRD da adjetivos; se anclaron a números preservando la jerarquía): json 0.99, jwt 0.95 (§6.5) · url/uuid 0.9 · base64 0.7 · unix_timestamp 0.6 · hash 0.4 · text 0.01. base64 longitud mínima = 1 cuanto (4). jwt exige header Y payload → JSON (más estricto que §6.2 literal, correcto: el payload de un JWT real ES JSON; la firma queda opaca). json excluye escalares desnudos (load-bearing para I8).
**REVIEW**: code-review (correctness) trazó la lógica contra §6.2 — sin bugs. Detalle clave verificado a mano: `1752624000` base64-decodifica a bytes con `EB 6E` (UTF-8 inválido) → U+FFFD → `isPrintableText` false → base64 declina → sale exactamente `[unix_timestamp, text]` (I8 se sostiene por el guard de contenido R4, no por casualidad). Mutua exclusión por construcción; orden desc estable (sort ES2019) → determinista (I5). simplify: 2 nits cosméticos → ambos SKIP (parseJsonObject devolver el objeto es forward-útil para las transformaciones json de T1.2; `void input` es patrón intencional documentado). ds-reviewer: N/A (sin superficie web).
**VERIFY (backend, verifier contexto fresco, probe independiente 17/17)**: PASS. R4 (`holaquetalestamos`→[text]; base64 binario→[text], el guard muerde por contenido no por longitud), I8 (`1752624000`→exactamente `[unix_timestamp,text]`), jwt+Bearer y header-no-JSON negativo, json excluye escalares, url/uuid/hash/unix positivos+negativos, text suelo 1×/último/0.01/desc, determinismo sin reloj, Zod valida de verdad (rechaza confidence fuera de [0,1] y kind inválido). Evidencia: `docs/verifications/T1.1/report.md` + probe + outputs crudos.
**Coste**: $0.
**Forward flag (no bloquea)**: los detectores usan `Buffer` (API de Node) — correcto para vitest/Node y el route handler de T1.4 en runtime Node; si algún día `POST /api/analyze` corriera en Edge runtime habría que portar a `atob`/`Uint8Array`. Anotado para T1.4.

## 2026-07-18 · T1.2 CERRADA — las 11 transformaciones del motor (PASS)

**Entrega**: `packages/core/src/engine/transforms.ts` — las 11 transformaciones de §6.3 como funciones PURAS y TOTALES (I1: nunca lanzan; fallo = `{ok:false,error}`), `buildTransforms(now)`/`buildTransformIndex(now)`, `DEFAULT_TRANSFORM_BY_KIND` + `defaultTransformId` (registro por defecto de §6.3 con la regla condicional de `url`). Gate verde: 313 tests.
**Inyección de `now` sin romper §6.1**: `Transform.apply` sigue siendo `(input)=>TransformResult`; las 2 transformaciones dependientes del tiempo (`timestamp.to_relative`, nota `exp` de `jwt.decode`) se construyen vía factory `buildTransforms(now)` cerrando sobre `now` → puras dado `now`. SIN default de producción: el reloj lo pasa el caller (T1.3), el motor jamás lo fabrica.
**Interpretaciones §6.3/§6.5 fijadas**: `jwt.decode` emite `{header,payload,signature}` COMPACTO (load-bearing: que `json.format` aporte en el paso siguiente de la cadena, §6.5); nota exp `exp: <ISO-corto-Z> (caducó hace X / caduca en X / caduca ahora)`; `to_iso` conserva ms; `to_relative` español hand-rolled («hace/en/ahora mismo», no `Intl.RelativeTimeFormat` que dice «dentro de»); `sort_keys` recursivo sin reordenar arrays; `url.split_query` claves repetidas→gana la última.
**REVIEW**: code-review (correctness) sin bugs (las 11 apply son totales; tiempo inyectado bien). simplify: 3 nits de reuso de bajo valor (HASH_CANDIDATES y regex base64 duplicados con detectors.ts; parseTimestamp solapa el shape del detector) → SKIP todos: son datos-hecho estables y consolidarlos editaría `detectors.ts` (superficie de T1.1 ya committeada) → refactor cross-task mejor hecho deliberadamente; mantener el diff de T1.2 acotado. ds-reviewer: N/A.
**Corrección menor del PRD (CLAUDE.md: cambio menor → editar en sesión y anotar)**: §6.5 decía `exp: 2026-07-16T00:00:00Z` pero el epoch `1752624000` del token = `2025-07-16T00:00:00Z` (verificado en runtime, `node -e`). Corregido `2026`→`2025` para que el ejemplo trabajado sea internamente consistente (importa: es el contrato exacto de la cadena de T1.3). El FORMATO de la nota es el contrato; el código DERIVA la fecha del `exp`, no la hardcodea. No es cambio de alcance, es fe de erratas.
**VERIFY (backend, verifier contexto fresco, script propio)**: PASS. Salidas exactas de las 11; jwt.decode §6.5 compacto + nota byte-idéntica; I1 (11×11 basuras, 0 excepciones); `to_relative` determinista; `buildTransforms` exige `now`; guard `no-clock` muerde (inyecté `Date.now()`→rojo, revertido sha-idéntico) sin falso positivo por comentarios; `defaultTransformId` correcto incl. url condicional y `text`→null. Rareza (no defecto): `url.decode("nope")→{ok:true,"nope"}` — I1 pide no-lanzar + `{ok:false}` para entradas ROTAS de esa transform; lo que la transform acepta legítimamente no es «roto», y el filtro R4 vive en el detector. Evidencia: `docs/verifications/T1.2/report.md` + script + gate.
**Coste**: $0.

## 2026-07-18 · T1.3 CERRADA — motor de cadena `analyze()` (PASS)

**Entrega**: `packages/core/src/engine/analyze.ts` — `analyze(input,{now})` que construye la `Chain` de §6.1 (detecta → aplica default → re-detecta → repite) sobre un bucle interno `runChain(input, detectFn, index)`. Implementa I1–I6 y la regla de §6.5 (`output===input` → `terminal:'no_transform'`, evaluada ANTES del guard de ciclo). Los 5 terminales: text/no_transform/error → `applied:null, output:null`; cycle → conserva `applied`+`output` del paso que cerró el ciclo; max_depth → sin paso terminal extra, el 8º paso mantiene applied/output reales y `steps.length ≤ 8`. Golden del corpus en `golden/analyze/` (incl. `jwt-6.5.json`). Gate verde: **354 tests / 33 ficheros**.
**§6.5 byte a byte**: `analyze("Bearer eyJ…", {now:2025-07-16T04:00:00Z})` → 3 pasos, `applied` = jwt.decode/json.format/null, `terminal:'no_transform'`, `notes[0] = ["exp: 2025-07-16T00:00:00Z (caducó hace 4 horas)"]`, paso0 JSON compacto + paso1 indentado. Verificado que el golden se genera de la salida REAL (`UPDATE_GOLDEN=1` byte-idéntico).
**REVIEW**: code-review (correctness) sin bugs; forma de los 5 terminales trazada contra §6.1. simplify: limpio (nits de bajo valor cross-task → SKIP, diff acotado). ds-reviewer: N/A (sin superficie web).
**Cláusula de ciclo — ATAQUE ADVERSARIAL (advisor, usuario dormido)**: la Verificación exige que "una entrada auto-alimentada en base64 termina en `terminal:'cycle'`". El verifier NO asumió imposibilidad: intentó CONSTRUIRLA (146 candidatos: base64 anidado, url.decode encadenado, JSON recursivo…) → **0 ciclos**. Prueba de imposibilidad para v1: solo `base64.decode`/`url.decode` encadenan y AMBAS encogen estrictamente (base64 de n bytes > n → decode nunca iguala/supera un input previo); único default por kind → camino determinista sin par inverso (minify nunca es default); `output===input` se evalúa antes del guard → auto-retorno es `no_transform`, no `cycle`. El **guard I3 sí está bien implementado y ejercitado sobre el `runChain` REAL** (inyectando solo las hojas detect/índice, igual que se inyecta `now`) con **control negativo que MUERDE** (guard neutralizado → el caso de ciclo cae a `max_depth` en vez de `cycle`). Veredicto: **PASS on substance** — el invariante I3 (ciclo→`cycle`, previos intactos) está verificado por el medio más fuerte disponible; solo el EJEMPLO de la cláusula es insatisfacible en v1. Decisión clave del advisor: **NO auto-enmendar el contrato para ponerse en verde** (eso sería «debilitar el test»); se cierra sobre el PASS sustantivo del verifier y se RUTEA la corrección de wording al usuario (abajo). Evidencia: `docs/verifications/T1.3/report.md` + candidatos + prueba + rojo/verde del control negativo.
**Coste**: $0.

### ⚠ 3 HUECOS DE CONTRATO (T1.3) → AÑADIDOS AL PAQUETE DE DECISIÓN (no bloquean; NO los edito yo — tu criterio sobre el contrato humano)
1. **La justificación de I3 en PRD §6.4 y el ejemplo de la cláusula de Verificación de T1.3 son FALSOS para v1**: "base64.decode se auto-alimenta → ciclo" no ocurre (base64.decode encoge → satura en `max_depth`, nunca cicla). La protección real contra base64 anidado es **I2/max_depth**, no I3. El guard I3 es correcto y defensivo (futuras transformaciones con par inverso sí podrían ciclar), pero su *racional escrito* y el *ejemplo de la cláusula* deberían corregirse. Propuesta de reformulación de la cláusula (a tu OK): «el guard I3 corta un grafo de transformaciones construido para auto-alimentarse a `terminal:'cycle'` conservando los pasos previos (verificado inyectando las hojas sobre el `runChain` real + control negativo)». **Decisión tuya** porque toca el racional de un invariante del contrato aprobado.
2. **§6.5 muestra `detections=[json]` en el paso 2**, pero `detect()` siempre añade el suelo `text` (I6) → real `[json, text]`. El golden refleja lo real; el ejemplo del PRD está incompleto. Fe de erratas menor, pero la dejo a tu OK por consistencia con el contrato.
3. **`ChainStep` gana un campo opcional `notes?: string[]`** no listado en §6.1, necesario para que la nota `exp` del JWT (§6.5 / criterio 14.1) llegue a la UI. §6.1 lista 5 campos; este es el 6º (opcional). A ratificar en §6.1.
> Ninguno es FAIL: la sustancia del motor verifica. Son correcciones del TEXTO del contrato (PRD/planning) que, por regla de CLAUDE.md (toca racional de invariante = cambio no-trivial del contrato humano), no aplico en solitario. **Siguiente tarea autónoma: T1.4 (`POST /api/analyze`)** — depende de T1.3+T0.1 (ambas cerradas), tiene superficie API pero sin UI, verificación por curl (413/429/grep de logs). Es la última tarea elegible antes de que T1.5 (UI) toque TD.7 (tu OK).

## 2026-07-18 · T1.4 CERRADA — `POST /api/analyze` (PASS)

**Entrega**: route handler `apps/web/src/app/api/analyze/route.ts` — público sin auth (D6), Node runtime + `force-dynamic`. Flujo: rate limit por IP → leer body → medir bytes (I7: >128 KB → **413 SIN procesar**) → `JSON.parse` → `parseOrThrow(AnalyzeRequestSchema)` → `analyze(input,{now:new Date()})` (I4: reloj en el borde, no en el motor) → `ChainSchema.safeParse` de SALIDA (PRD §6.1) → `Response.json`. Nuevos: `apps/web/src/server/rate-limit.ts` (ventana deslizante en memoria + accessor lazy con override para tests, molde de `getDb`/`getRootLogger`; 60/min por defecto, `ANALYZE_RATE_LIMIT`/`ANALYZE_RATE_WINDOW_MS` por env). Extensiones de contrato mínimas y sancionadas: `AnalyzeRequestSchema` (`{input:string}`, admite `""`) en `@app/core/engine`; code `payload_too_large`→413 en `@app/core/contracts` (architecture §5). `parseOrThrow` exportado de `with-route.ts` (2º consumidor). Gate verde: **363 tests / 35 ficheros**.
**§11 (regla núcleo — el input NUNCA se loguea) por capas**: `analyze_completed` emite solo `input_kind`/`input_bytes`/`steps`/`duration_ms`; el 413 loguea solo tamaños; el drift de salida usa marcador estático (NUNCA el `chain`, cuyo `steps[0].input` ES el input); `parseOrThrow` usa `z.flattenError` (sin valor recibido); `readJson`/JSON.parse capturan y loguean solo `err_name` (el `message` de SyntaxError lleva un prefijo del input → descartado). Mensajes de `AppError` estáticos.
**REVIEW**: code-review (correctness) — sin bugs; auditadas todas las rutas de §11 (handler + with-route + parseOrThrow + readJson) → el input no puede filtrarse por ninguna. simplify: nada accionable (reuso de parseOrThrow/accessor lazy/withRoute; unión de error codes extendida no duplicada; 413 con code de contrato, no bandaid). ds-reviewer: N/A (API sin superficie de navegador).
**Decisiones fijadas**: 413 medido por `Buffer.byteLength` del body tras `req.text()` (no `content-length`, que un `Request` a mano no lleva) → curl del verifier y test recorren la misma rama; rate limit ANTES del 413 (un abusador no fuerza buffering) → una petición 413 igual consume cuota; identificación de IP **provisional** vía `x-forwarded-for` con nota `// T3.1:` (trust boundary real `CF-Connecting-IP`+`TRUST_PROXY=1` en T3.1). Test handler-level bajo `web:unit` (sin BD, precedente de health); `pnpm test:e2e` N/A (API sin UI; la UI llega en T1.5).
**VERIFY (curl real contra server levantado, verifier contexto fresco)**: PASS. §6.5 sin sesión → 200 con Chain exacta (`ChainSchema` ok); 200014 B → 413 y su `request_id` NO aparece en ningún `analyze_completed` (no procesado); 429 determinista con `ANALYZE_RATE_LIMIT=2` + mismo XFF (200,200,429); **grep de 8 centinelas/fragmentos (JWT header+payload, sentinels, relleno 200 KB, "Bearer eyJ") sobre TODO el log → CERO coincidencias** (criterio 14.9); métricas §11 presentes; rutas de error (JSON roto, tipo inválido) → 400 sin eco del body. Evidencia: `docs/verifications/T1.4/report.md` + `raw/`. Gotcha de secuenciación (bucket `unknown` compartido + token antes del 413) documentado y sorteado con `ANALYZE_RATE_LIMIT=2`.
**Coste**: $0.
**Observaciones ruteadas (no bloquean)**: (1) IP provisional → T3.1; (2) code `payload_too_large` añadido a `@app/core` (revisable si el equipo prefiere otro nombre). Ambas anotadas en el report.

### ⏹ PARADA DEL BUCLE — fin del tramo autónomo (T1.5+ tocan tu OK)
Con T1.4 cerrada, el grafo `Depende de` NO deja ninguna tarea autónoma más:
- **T1.5** (pantalla `/`) depende de **T1.4 ✓ + TD.7 + TD.5 ✓** → bloqueada por **TD.7** (cierre de fase TD, que a su vez depende de TD.4 = tu decisión de gaps + autorización de subida a Claude Design, y de tu **revisión humana visual** del showcase).
- **T1.6** (alternativas/desvío de cadena) depende de T1.5.
- **F0 entera** (T0.2→T0.5: Docker/Postgres/auth) sigue aguas abajo de TD.7 (T0.2 depende de ORDEN de TD.7).
- **T3.1** (trust boundary) depende de T0.4 (F0, bloqueada).
→ Toda la runway restante pasa por **TD.4/TD.7 = tu criterio humano**. El motor de F1 (T1.1–T1.4) está completo, verificado y pusheado. **El bucle se detiene aquí y espera tu decisión** (ver el PAQUETE DE DECISIÓN de arriba: gaps de TD.4, autorización de subida, y las 3 deudas de DS; + los 3 huecos de contrato de T1.3 a ratificar).

## 2026-07-18 · Ratificación de contrato (T1.3) + arranque de TD.4 tras OK del usuario

El usuario aprobó ("todo me parece ok, dale") los 4 puntos del PAQUETE DE DECISIÓN. Aplicadas las **3 ratificaciones de contrato de T1.3** (edición de PRD/planning con su OK — no debilita el test: T1.3 cerró en verde contra la cláusula SIN enmendar, esto es documentación):
- **PRD §6.1**: `ChainStep` gana `notes?: string[]` (la nota `exp` del JWT que la UI consume, ya presente en `TransformResult`).
- **PRD §6.4 I3**: corregido el racional falso ("base64 se auto-alimenta"): con las transformaciones de v1 el ciclo es INALCANZABLE (base64/url.decode acortan; sin par inverso entre defaults → base64 anidado satura en `max_depth` vía I2, no `cycle`); el guard I3 es defensivo para futuras transformaciones y se verifica inyectando un grafo auto-alimentado sobre el bucle real.
- **PRD §6.5**: paso 2 `detections=[json 0.99]` → `[json 0.99, text 0.01]` (suelo `text` de I6, coherente con pasos 0/1).
- **planning T1.3**: cláusula de ciclo reformulada al guard-sobre-runChain + control negativo (el que ya se verificó).
Gate verde (363). Commit propio de docs.

**Plan aprobado para desbloquear la fase TD** (orden fijado con el advisor):
- **TD.4**: crear **Dialog** (confirmación, T2.2) + **Wordmark** (header `/` + auth); diferir Toast/Tooltip/Skeleton. Base del Dialog = **`<dialog>` NATIVO** (`showModal()` da focus-trap/Escape/`::backdrop`/`aria-modal` sin librería) — esquiva el veto de `@radix-ui/*` de TD.6 y sigue el precedente del `Select` nativo (frontend §4). Autoría en AMBOS formatos (fuente `.jsx`/`.d.ts`/`.prompt.md`/card en el espejo + mirror `.tsx` en `components/ui/`), showcase, tests; el blink del Wordmark PARA bajo `prefers-reduced-motion`. La **subida a Claude Design vía DesignSync** la ejecuta el bucle (paso externo irreversible, autorizado por el usuario), con `get_project` confirmando `PROJECT_TYPE_DESIGN_SYSTEM` y parity `list_files` como gate del verifier. Hand-authoring del espejo = excepción sancionada de TD.4 (componentes nuevos sin specimen upstream).
- **Deudas de DS (punto 3) ANTES de TD.7** (el humano las vería en la revisión visual): Button `--danger-hover` y Field `aria-describedby` = mecánicas, se arreglan; violet/cyan oscuro = derivar valores AA y MOSTRARLOS en el showcase (identidad de color = no se commitea en silencio).
- **TD.7**: cierre de fase + revisión humana visual (parada de fin de fase).

## 2026-07-18 · TD.4 CERRADA — Dialog + Wordmark + subida a Claude Design (PASS)

**Entrega**: las 2 primitivas fuera del inventario del DS que el producto hará inevitables (Toast/Tooltip/Skeleton DIFERIDAS por decisión del usuario — sin consumidor duro). **Dialog** — modal de confirmación sobre el elemento **`<dialog>` NATIVO** (`showModal()`/`close()`): foco atrapado, Escape, `::backdrop`, `aria-modal` y restauración de foco SIN librería (0 `@radix-ui`/`lucide-react` — TD.6 los veta y no hay primitiva instalada). Mismo patrón sancionado que `Select` (control nativo cuando a11y+plataforma lo apuntan, frontend §4). API controlada `open`/`onOpenChange`/`onConfirm`/título/descripción/`confirmTone`(`primary|danger`); compone el `Button` del DS; foco inicial en «Cancelar». **Wordmark** — marca mono `devtools` + cursor de acento parpadeante, `size`/`blink`, blink apagado bajo `prefers-reduced-motion` conservando visibilidad (disciplina de TD.5). Ambas en AMBOS formatos: fuente del espejo (`docs/design-system/components/{overlay,brand}/*.jsx|.d.ts|.prompt.md` + `*.card.html` con `@dsCard`) + mirror `.tsx` (`apps/web/src/components/ui/{dialog,wordmark}.tsx`) + secciones nuevas en `/design-system` + tests. Gate verde: **377 tests / 37 ficheros**.
**Subida a Claude Design (DesignSync, ejecutada por el bucle — paso externo autorizado por el usuario)**: `get_project` confirmó `PROJECT_TYPE_DESIGN_SYSTEM`+`canEdit` del proyecto DevTools Design System (`9d6b478a-…`); `finalize_plan`→`write_files` subió 9 ficheros (los 8 de componente + `readme.md` con el índice ampliado a overlay/brand para «inventario completo»); `list_files` confirmó paridad remoto↔espejo local. Los card «Wordmark» de `guidelines/` (Brand, foundation) y `components/brand/` (Components, componente) coexisten a propósito. Hand-authoring del espejo = excepción sancionada de TD.4 (componentes nuevos sin specimen upstream). El bucle completó el índice del readme (2 líneas) para mantener el inventario coherente.
**REVIEW**: code-review (correctness) sin bugs (sync `open`↔`<dialog>` con guard, backdrop-dismiss por `e.target===dialog` con padding en el hijo, blink cae la clase con `blink=false`). ds-reviewer: **LIMPIO** (compone Button, tokens resuelven, props 1:1 con `.d.ts`, fuente↔mirror coherente; scrim `code-bg/70` sancionado — deuda: el DS no tiene token de scrim). simplify: 1 nit → arreglado en la fuente `.jsx` (mapa identidad `TONE` muerto → `variant={confirmTone}` directo, antes de subir).
**VERIFY (CUA navegador real + gate, verifier contexto fresco)**: 1ª pasada FAIL en 1 punto → el `<dialog>` modal se anclaba arriba-izquierda (0,0) porque el reset `margin:0` de Tailwind anula el `margin:auto` con que el navegador centra un `dialog:modal`; jsdom no posiciona `showModal()` (polyfill de solo lógica) → lo cazó el gate CUA, no los unit. **Fix**: `m-auto` en las clases del `<dialog>`. **RE-VERIFY** (CUA, ambos temas): PASS — panel centrado [640,289] vs viewport [640,288.5] (±1px, margin auto = 207.875/430px), foco inicial en Cancelar, Escape/backdrop cierran, clic dentro no cierra; +reduced-motion (blink para, visible), contraste ≥4.5, `list_files` con los 8 nuevos, sin radix/lucide en el mirror. **TD.4 = PASS global**. Evidencia: `docs/verifications/TD.4/report.md` + 7 capturas + mediciones.
**Coste**: $0.
**Interludio (usuario)**: pidió deploy mid-TD.4; investigado → NO hay `docker-compose.prod.yml`/Dockerfile/Postgres (es infra de F0, aguas abajo de TD.7), `deploy.env` sí está relleno (devtools.carlosvillu.dev:3110, VPS 80.190.75.149). Surfaced el false-premise + fork al usuario (advisor confirmó: es decisión suya, no completable sin él — host-key, DNS/SSL, sudo). Usuario: «olvida lo del deploy». No se construyó infra fuera de orden. Loop retomado.
**Siguiente (orden fijado con advisor)**: deudas de DS (Button `--danger-hover`, Field `aria-describedby`, contraste violet/cyan oscuro con valores AA a mostrar) ANTES de **TD.7** (cierre de fase TD + revisión humana visual = parada de fin de fase).

## 2026-07-18 · Deudas de DS remediadas (PASS) — antes de TD.7

Las 3 deudas de DS aprobadas por el usuario ("tanda acordada"), arregladas source-first (fuente del espejo + mirror `.tsx`/`.css` 1:1) y subidas a Claude Design. Se hacen ANTES de TD.7 porque el humano las vería en la revisión visual (advisor).
1. **Button danger-hover**: la FUENTE `Button.jsx` había driftado a rampa cruda `var(--red-700)` en hover; el mirror `.tsx` ya consumía `hover:bg-danger-hover` desde TD.2. Convergen ahora a `--danger-hover` (=red-700, más oscuro que `--danger`=red-600). 
2. **Field aria-describedby**: `useId` → ids estables `${id}-hint`/`${id}-error`, inyectados en el control vía `cloneElement`, fusionando el `aria-describedby` que traiga el caller; `error` tiene prioridad sobre `hint` (un solo nodo descrito). Contrato de props intacto. +4 tests con control negativo.
3. **★ Contraste violet/cyan oscuro (WCAG AA)**: alias semánticos theme-aware NUEVOS `--violet-subtle-fg`/`--cyan-subtle-fg` (en oscuro → step `-100`, in-family con success/warning/danger); solo cambia el color de TEXTO del badge, solo en oscuro. **json 2.11→12.58, cyan 2.49→12.24; claro intacto (6.24/5.17)**. Bonus: al ser token semántico y no rampa cruda, se retiraron los 2 `eslint-disable` sancionados de `badge.tsx`. Test permanente nuevo `globals.contrast.test.ts` (lee valores reales, ambos temas, control negativo). Valores oscuros elegidos: violet `--violet-100` oklch(0.930 0.050 295), cyan `--cyan-100` oklch(0.930 0.045 210) — el `-100` existe en la rampa; `-200`/`-300` no, y añadirlos sería más superficie. **Identidad preservada** (sigue siendo violet/cyan, un hue por kind).
**REVIEW**: code-review (correctness) sin bugs (merge de `aria-describedby` correcto, prioridad error/hint, guard `isValidElement`). ds-reviewer: LIMPIO (alias coherentes con el patrón subtle-fg, rampas `-700` intactas → sin regresión de swatches, `@theme` mapea los alias, disables retirados correctamente). simplify: 1 nit opcional (un solo id en Field en vez de hint/error) → SKIP (los ids explícitos son auto-documentados; tocar ambos formatos por 2 líneas no compensa).
**DesignSync (bucle)**: subidos los 4 ficheros fuente cambiados (`components/display/Badge.jsx`, `components/forms/Button.jsx`, `components/forms/Field.jsx`, `tokens/colors.css`); paridad remoto↔espejo confirmada. **DEUDA CONOCIDA (no bloquea, se auto-cura)**: `_adherence.oxlintrc.json` (remoto y local) quedó STALE — no lista `--violet-subtle-fg`/`--cyan-subtle-fg`/`--danger-hover` ni los componentes overlay/brand de TD.4. Es un artefacto GENERADO por el self-check del panel de Claude Design (no por `write_files`), y NO lo consume el gate real (`eslint.config.ts` §5c). Se refrescará solo cuando el panel corra su self-check (p. ej. al abrirlo en la revisión de TD.7). NO se edita a mano (regla: el espejo jamás se edita a mano).
**VERIFY (verifier contexto fresco: cálculo WCAG independiente + navegador + test efímero + gate)**: PASS. Contraste medido oscuro json 12.58 / cyan 12.22, claro 6.25/5.12, control negativo muerde (2.11/2.49); Button danger consume `--danger-hover` (vía fuente+mirror+CSS compilado — el `:hover` sintético de CDP no dispara restyle, afecta igual al primary → no es defecto); Field enlaza hint/error con fusión y prioridad. Gate verde 384/38. Evidencia: `docs/verifications/ds-debts/report.md`.
**Coste**: $0.
**Siguiente**: **TD.7** — cierre de fase TD: actualizar la skill `frontend` contra el código real + E2E visual de `/design-system` + **revisión humana final del showcase** (parada de fin de fase, el usuario da el OK visual). El DS queda ya AA-compliant para esa revisión.

## 2026-07-18 · TD.7 CERRADA + FASE TD COMPLETA (PASS, OK visual humano dado)

**Entrega**: skill `frontend` actualizada contra el CÓDIGO REAL committeado — `references/design-system.md` §4.1 (inventario de las 18 primitivas de `components/ui/` con variantes cva/props leídas de los `.tsx`) + §4.2 (desviaciones deliberadas: Select/Dialog nativos, `--shadow-*` 1:1, px inline del Wordmark, alias `subtle-fg`, Badge/Callout sin cva, etc.), obligatoriedad explícita reforzada, placeholders `{{PROJECT_NAME}}`/`{{CLAUDE_DESIGN_URL}}` sustituidos, y una contradicción skill↔código corregida hacia el código (`--shadow-*: var(--shadow-*)` 1:1 vía `@theme inline`, sin `var()` circular — la prescripción `--elevation-*` de la skill NO era necesaria). Ajustes de fase anotados en el journal.
**VERIFY (E2E de fase, verifier contexto fresco: CUA ambos temas + gate + skill↔código)**: PASS de la parte automatizable. Gate verde 384/38; recorrido completo de `/design-system` en claro y oscuro sin defectos (badges DataKind AA en oscuro: json 12.58/base64·uuid 12.22/jwt 10.16/text 5.71; Dialog centrado (640,289); Wordmark blink + parada reduced-motion; consola limpia); skill §4.1/§4.2 casa 1:1 con el código (spot-check Button/Dialog/Badge/Field/Select); placeholders sustituidos; aserción `--shadow-*` cierta. Evidencia: `docs/verifications/TD.7/report.md` + 16 capturas por sección/tema.
**★ REVISIÓN HUMANA (parada de fin de fase)**: se presentaron al usuario las capturas del showcase completo en ambos temas + detalle de badges oscuros y Dialog. **El usuario dio el OK visual explícito ("Todo guay. Dale")**. Con eso TD.7 y la FASE TD quedan CERRADAS.
**Coste**: $0.

### ✅ FASE TD (Design System) COMPLETA
TD.1 tokens/showcase · TD.2 primitivas de formulario · TD.3 display/feedback · TD.4 gaps (Dialog+Wordmark, subidos a Claude Design) · TD.5 composites puros · TD.6 lint de adherencia · TD.7 skill+E2E+OK humano. + deudas de DS saldadas (contraste AA, danger-hover, aria-describedby). El DS está completo, verificado, AA-compliant y espejado en Claude Design.
**Desbloqueos**: TD.7 destraba **T0.2** (dependencia de ORDEN → toda la cadena F0: Docker/Postgres/auth) y **T1.5** (pantalla `/`, depende de T1.4✓+TD.7✓+TD.5✓ — no necesita F0, `/` es pública D6 y solo consume `POST /api/analyze`). Próximo ciclo: elegir siguiente tarea por el grafo (F0 infra vs T1.5 primera UI del motor).
**Pendiente menor (anotado, no bloquea)**: el patrón `{{…}}` sin sustituir vive aún en OTRAS skills (testing, deploy, bootstrap, postgres-drizzle, pnpm, vercel-*) — muchas son plantillas de terceros/arnés; tratar aparte cuando cada fase las toque. `_adherence.oxlintrc.json` del espejo stale (se autocura al abrir el panel de Claude Design).

## 2026-07-18 · T1.5 CERRADA — la pantalla `/` (campo + cadena) (PASS)

**Entrega**: la pantalla principal `/` (§7) desde `docs/mockups/field.html` con las primitivas del DS. Textarea con foco automático; análisis **al pegar (inmediato)** o **tras 300 ms al escribir (debounce)**, sin botón, **cancelando el fetch en vuelo** (AbortController + contador de secuencia → una respuesta lenta y vieja no pisa una entrada nueva). La cadena debajo: `ChainSummary` (kinds distintos-consecutivos) + un `StepCard` por paso (tipo, transformación, valor en CodeBlock con CopyButton, notas como la expiración natural, marcador terminal en el último). `Callout` de seguridad §11 (copy literal). Entrada no reconocida (`isUnrecognized` = ningún paso aplicó transform) → Callout explícito con qué se intentó, nunca pantalla vacía. Estrenado el **arnés Playwright e2e** (primera vez en el repo).
**Arquitectura (frontend skill)**: página Server Component delgada (`page.tsx`) + hoja `'use client'` (`components/field/field-analyzer.tsx`); fetch por `lib/api-client.ts` (cliente tipado isomorfo, valida la respuesta con `ChainSchema`, envelope→`ApiError`); wrapper de dominio puro `components/field/chain-to-step-cards.ts` (traduce `Chain`→props de los composites PUROS de TD.5; vive fuera de `components/chain/` porque el lint de TD.5 prohíbe importar `@app/core` ahí). `components/layout/site-header.tsx` con Wordmark. Nuevos scripts: `test:e2e` + `playwright.config.ts` + `scripts/e2e-stack.ts`.
**Decisiones fijadas**: JWT emite 3 StepCards (el motor da 3 pasos: jwt.decode→json.format→terminal `no_transform`) — honesto con el motor, el layout casa 1:1 con el mockup (el `CHAIN_JWT` de 2 del mockup es script de ejemplo). Puerto e2e **3118** (no 3100): este entorno ES el VPS y el vecino `ugc.carlosvillu.dev` ocupa 3100 — con `reuseExistingServer` la suite testeaba la app del vecino. Stack e2e mínimo sin BD (/ es pública D6, no toca Postgres; crecerá en F0). Tema oscuro en `/` alcanzable por `data-theme="dark"` (el mockup no trae toggle ni auto-switch; coherente con el resto de la app — decisión de producto si se quiere dark automático).
**REVIEW**: code-review (correctness) sin bugs (debounce/abort/seq correcto y con limpieza al desmontar; api-client isomorfo bien; `isUnrecognized` distingue texto-plano de timestamp-que-acaba-en-text por «¿se aplicó algo?»). ds-reviewer: LIMPIO (Textarea/Wordmark/StepCard/ChainSummary/CopyButton/Callout/Spinner/Kbd/Icon del DS; cero HTML crudo reemplazable; cero tokens hardcodeados; props en contrato). simplify: 3 nits → SKIP con causa (api.get par canónico get/post, consumidor GET llega en T1.6; `useMemo` PROHIBIDO por la skill — React Compiler activo; rama `NODE_ENV==='test'` load-bearing para los tests de componente en jsdom).
**AA fix post-verify (en mi código, no DS)**: el verifier ruteó que el chip decorativo «N pasos · terminal» usaba `text-success` (green-600) a 12px sobre fondo claro = **3.93:1 < 4.5 AA**. Arreglado sin tocar el DS: verde solo en el ICONO (objeto gráfico, umbral 3:1 → 3.92 ✓) y el TEXTO a `text-text-muted` (**7.25:1 ✓**, verificado por cálculo oklch→sRGB). Sin decisión de identidad de color.
**VERIFY (navegador real CUA + gate + e2e, verifier contexto fresco)**: PASS. Foco automático; 14.1 (JWT<1s → `jwt→json`, payload indentado + `exp: 2025-07-16… (caducó…)`, sin botón); 14.2 (base64→JSON = 3 pasos, copia de intermedio con 1 clic = `{"a":1,"b":[2,3]}`); no-reconocido explícito; Callout de seguridad; móvil apila sin scroll horizontal; coherencia con el mockup ambos temas; **§11 grep del centinela sobre logs = 0**; consola limpia. Gate 406/41, e2e 5/5. Evidencia: `docs/verifications/T1.5/report.md` + 9 capturas.
**Coste**: $0.
**Siguiente**: **T1.6** (alternativas de detección + desvío de cadena sobre esta pantalla — I8/O4/O5: mostrar detecciones descartadas con ConfidenceBar cuando confianza ≥0.3, y elegir transformación alternativa que recalcula desde ese paso). Depende de T1.5 ✓.

## 2026-07-18 · T1.6 CERRADA — alternativas de detección y desvío de la cadena (PASS)

**Entrega**: la interacción que cierra O4 y O5 sobre `/`. Cada `StepCard` muestra las **detecciones descartadas** clicables («También podría ser: …») y —cuando el kind tiene >1 transformación— un **picker** para elegir otra; al elegir alternativa (O5) o transformación (O4), la cadena se **recalcula desde ese paso dejando intactos los anteriores**.
**Approach (fijado con el advisor, no improvisado)**: la recalculación es un **REPLAY-con-overrides desde el inicio en el motor**, NO un recorte en cliente. Razón dura: I2 (≤8), I3 (ciclos) e I5 (determinismo) son invariantes de cadena COMPLETA — un splice daría a la cola un presupuesto de 8 y un seen-set FRESCOS, así prefijo(5)+cola(8)=13 > 8 (viola 14.7) y pierde ciclos del prefijo. El replay es el MISMO bucle único → los tres se conservan gratis y «pasos previos intactos» sale del determinismo. Test de control con grafo inyectado (`analyze.overrides.test.ts`): un desvío en el paso 5 sigue capado en 8 y el assert de splice se pondría ROJO (5+cola=13).
**Mecanismo único, tres entradas de UI**: `overrides: {step, transform|kind}[]` (unión Zod, `.max(8)`). O4 = fuerza id; O5-kind-X = default de X resuelto **en el servidor**; O5-`text` = `kind:'text'` ⇒ terminal (así se «elige texto», 14.3). El cliente mantiene la lista, en cada desvío la trunca a `step < N` y añade el nuevo; input nuevo ⇒ reset. Opciones del picker desde `transformsForKind()` (registro `SPECS`), no lista a mano.
**§11 intacto**: los overrides son ids/kinds/índices, nunca el input; el route los valida con Zod y no los loguea. Verificado por grep externo sobre el stdout del dev server (input crudo/firma/overrides = 0).
**REVIEW**: code-review (correctness, yo) — motor limpio: un override con id inexistente o transform que no resuelve cierra como `text` (total, sin crash); `.find` sobre ≤8 determinista; el desvío no puede saltarse I2/I3. simplify — 2 hallazgos: (1) **[medium, APLICADO]** `CONVIVES_WITH_TEXT` re-codificaba en la UI un hecho de detectores → movido a **`KINDS_COEXISTING_WITH_TEXT` en `core/detectors.ts`** (verificado antes que NO es derivable de la salida de `detect()`: `text` es el suelo de TODO input I6, así que su mera presencia no distingue timestamp/hash de jwt/json); (2) **[low, SKIP]** el `0.3` triplicado — el de `confidence-bar`/`design-system` es una frontera de COLOR, el `ALTERNATIVE_MIN_CONFIDENCE` es el umbral de INCLUSIÓN de I8: preguntas distintas, acoplarlas en una constante mentiría. ds-reviewer: **LIMPIO** — el `<button>` que envuelve el Badge para hacer la alternativa clicable (O5 «a un clic», R1) es un hit-target de foco puro (`focus-visible:ring-2 focus-visible:ring-ring`, token) sin chrome; el DS no tiene primitiva «badge interactivo» (candidata a promover en Claude Design vía DesignSync). El espejo `_adherence.oxlintrc.json` lista `onSelectTransform` pero no `onSelectAlternative` (desfase read-only, se autocura al regenerar; no lo consume el gate — el gate real de adherencia es `eslint.config.ts`).
**`isUnrecognized` refinado (toca semántica de T1.5, INTENCIONAL)**: de «ningún paso aplicó transform» → «la detección del paso 0 es `text`». Necesario porque el override force-text deja `applied=null` con detección real (p. ej. `unix_timestamp`): con la vieja definición, ese caso se ocultaría tras el callout «no reconocido» en vez de mostrar el paso. Verificado que **NO hay regresión de T1.5**: `text` es el ÚNICO kind con default null (`DEFAULT_TRANSFORM_BY_KIND`), así que para cualquier input REAL `det[0]===text ⟺ applied===null` — las dos definiciones coinciden en todos los inputs de T1.5 y solo divergen en el caso override (superficie nueva de T1.6). El diff de `chain-to-step-cards.test.ts` es **puramente aditivo** (ninguna aserción de T1.5 debilitada/borrada). La nueva definición es además más correcta.
**Reconciliación de la Entrega (menor, anotado)**: la Entrega decía «detecciones descartadas con su ConfidenceBar»; el `StepCard.jsx` del DS (fuente de verdad visual) pone el ConfidenceBar en la detección ELEGIDA y las descartadas como Badge chips. La implementación sigue el DS 1:1 → confianza SÍ visualizada (bar en el paso), ambigüedad SÍ visible y a un clic (badges). I8 y la Verificación literal cumplidos; el ConfidenceBar-por-alternativa habría sido más peso visual del que el DS pretende.
**Ratificaciones de contrato en el PRD** (como en T1.3/T1.4): §6.4 **I8** — regla de visualización (descartadas ≥0.3 + `text` si el kind convive con text §6.2; el set vive en el motor); §8 **analyze** — entrada pasa a `{input, overrides?}`, replay-con-overrides, §11 preservado, cota 8 (I2). `AnalyzeOptions` gana `overrides?` (contrato de código).
**VERIFY (verifier contexto fresco: navegador REAL agent-browser + gate + e2e)**: PASS. 14.3 (`1752624000` → badge timestamp + `2025-07-16T00:00:00.000Z`; «También podría ser: text» pese a conf 0.01 < umbral; elegir text recalcula 2→1 paso, terminal «Dato de texto»); 14.4 (json.format→json.sort_keys en el paso 1, cola recalculada, paso 0 jwt.decode intacto `"name":"carlos"`); cota I2 8/8; §11 grep externo = 0. Gate 432/42, e2e 7/7 (2 nuevos + 5 de T1.5 sin regresión). Evidencia: `docs/verifications/T1.6/report.md` + 3 capturas + logs.
**Rareza (no bloqueante)**: el picker se muestra también en el paso terminal `no_transform` (json tiene >1 transform) — coherente con el contrato de StepCard, y de hecho útil (re-rutear un json terminal a minify/sort). Sin efecto en la Verificación.
**Coste**: $0.
**Siguiente**: **T1.7** (E2E de fase F1) — depende de T1.6 ✓. Es el E2E de fin de fase F1: al cerrarlo, **parada de fin de fase** para resumen + OK humano (regla del bucle). Alternativa por el grafo: F0 (T0.2 Docker/Postgres) sigue elegible; el interés de deploy del usuario vive en esa rama si vuelve.

## 2026-07-18 · T1.7 CERRADA — E2E de fase F1 (PASS)

**Entrega**: el recorrido de fase `apps/web/e2e/phases/f1.spec.ts` (tags `@f1 @phase`, 6 tests) que ejerce los 5 casos de uso sin cuenta de §3 (CU1–CU5) contra el sistema real (UI ↔ `POST /api/analyze` ↔ motor). Solo se creó ese fichero: **cero código de producto tocado** (F1 ya estaba implementada en T1.1–T1.6; esta tarea es el gate holístico de la fase). Cobertura e2e NUEVA real: **14.5** (413 sobre ~200KB) y **CU5** (URL con params); 14.1–14.4 se re-ejercen a nivel de fase.
**Reparto de criterios (fijado con el advisor)**: 14.1–14.5 en el navegador (`f1.spec.ts`); **14.6 (determinismo I5) y 14.7 (≤8 pasos/sin ciclo I2/I3) NO son de navegador** — son invariantes del corpus del motor, ya cubiertos por `analyze.test.ts` (determinismo `it.each(CORPUS)` + golden byte-a-byte del §6.5; cota 8 + max_depth + guard de ciclos) y `analyze.overrides.test.ts` (cota en el camino desviado). El report los cita; no se reimplementan en Playwright.
**Decisión de test (14.1 «< 1 s»)**: el spec NO asserta la latencia de pared (flaky perpetuo contra un build de prod en un VPS con carga); protege el núcleo funcional (la cadena aparece sin pulsar nada, auto-wait). La MEDICIÓN literal la hizo el verifier: **166/104/76 ms** in-page → < 1 s con holgura. Buen reparto: el spec permanente no es flaky y el criterio queda medido con evidencia.
**Reconciliación de prosa §3 CU4 (ratificada en PRD)**: la narrativa literal («base64 decodifica a texto, el usuario elige hash a mano») está superada por el motor real, que **auto-detecta `hash`** para cualquier hex de 32/40/64 — nunca se queda en `text`. CU4 se realiza por su forma canónica **O4/14.4** (desviar un paso con >1 transformación); el divert no asigna kinds arbitrarios que el detector no propuso. Editado §3 CU4 del PRD. No es gap de producto (el PRD ya tagueaba 14.4 como «CU4, O4»).
**Nota de regresión N/A (confirmada)**: solo existen `field.spec.ts` y `field-alternatives.spec.ts` (ambos @f1); **no hay E2E de F0 ni spec permanente del showcase TD** (TD.7 fue CUA, no committeó spec), así que las cláusulas «sin regresión F0/TD» de la ficha son N/A.
**VERIFY (verifier contexto fresco: navegador real agent-browser + gate + e2e + suite del motor)**: PASS. 14.1 (166 ms, expiración natural, sin botón); 14.2 (3 pasos, copia intermedio 1 clic); 14.3 (timestamp + alternativa text + recalcula); 14.4 (json.format→json.sort_keys, paso 0 jwt/«carlos» intacto); 14.5 (curl 204813 B → 413 `payload_too_large`, **log solo rechazo, 0 `analyze_completed`** = sin procesar, I7); CU5 (url.split_query → «abc xyz»/«a/b+c»/«https://foo.com» decodificados); 14.6/14.7 verdes en la suite del motor; **§11/14.9 grep de los 5 inputs sobre los logs = 0**. Gate 432/42, e2e 13/13 (6+5+2, sin regresión). Rareza no bloqueante: el clipboard del sandbox VPS es inestable → para CU2–CU5 el verifier sembró el texto con un evento `paste`+`input` real que recorre el código de producto íntegro (solo sustituyó el transporte de clipboard, ya probado por el pegado nativo de CU1 y los 13 e2e); los clics/selects que SON el objeto de cada CU se hicieron con comandos reales. Evidencia: `docs/verifications/T1.7/report.md` + 7 capturas + logs (curl, log de 14.5, grep §11).
**Coste**: $0.

### ✅ FASE F1 (El motor y el campo) COMPLETA
T1.1 contratos+detectores · T1.2 transformaciones · T1.3 motor de cadena · T1.4 `POST /api/analyze` · T1.5 pantalla `/` · T1.6 alternativas+desvío (O4/O5) · T1.7 E2E de fase. **El primer hito de valor real**: pegas algo en `/` (público, sin cuenta) y ves la cadena desenredada, con alternativas y desvío. Criterios 14.1–14.7 cerrados y verificados; §11 blindado (grep = 0). 15/25 tareas.
**★ PARADA DE FIN DE FASE**: procede presentar el resumen de F1 al usuario y esperar su OK antes de F2, según el protocolo del bucle.
**Siguiente por el grafo (tras el OK)**: **F2** (historial: T2.1 depende de T0.4+T1.4 → necesita F0/auth primero) o **F0** (T0.2 Docker/Postgres, ya elegible, destraba auth y el interés de deploy del usuario). El orden real F0→F2 lo impone el grafo: el historial necesita cuenta, y la cuenta necesita Postgres.

## 2026-07-18 · T0.2 CERRADA — Docker Compose de desarrollo con Postgres (PASS)

**Entrega**: `docker-compose.dev.yml` (`postgres:16`, volumen `devtools-pg-data`, healthcheck `pg_isready`, publicado en **`127.0.0.1:5433`**) + `.env.example` (raíz, para compose) + `apps/web/.env.example` (para Next en el host). `GET /api/health` pasa a `{ok, db}`: sonda mínima `apps/web/src/server/db-health.ts` (pool `pg` perezoso, `SELECT 1`, timeout 2s, **traga cualquier fallo → `db:false`, nunca lanza**), contrato `HealthSchema={ok:true, db:boolean}` de core. Primera pieza de la fase **F0**.
**Contexto VPS (crítico)**: este entorno ES el VPS de producción compartido — `docker ps` muestra vecinos vivos `ugc-factory-{web,postgres,worker}` + `edge-caddy`. Salvaguardas aplicadas: proyecto compose fijado a `devtools` (contenedores `devtools-*`), Postgres publicado SOLO en **loopback 5433** (no 0.0.0.0 → no se salta UFW; no 5432 → no roza al vecino), y todo `up/down` acotado al proyecto devtools. Registrado en `~/AGENTS.md` (fuente de verdad del VPS): devtools + puerto dev-postgres 5433. Verificado que los vecinos siguen vivos antes/después.
**Decisiones no obvias**: (1) la web corre en el HOST (no en el compose) y Next lee `DATABASE_URL` de **`apps/web/.env`**, no del `.env` de la raíz (Next carga env del dir del paquete) — descubierto empíricamente; los dos `.env.example` documentan el reparto. (2) «Conecta al arrancar» = accessor **perezoso**, no conexión eager en boot: una conexión eager podría lanzar en el arranque con Postgres caído y tumbar la web — justo lo que prohíbe el control negativo. (3) La sonda vive en `apps/web/db-health.ts`, no en `packages/db` (que sigue vacío hasta T0.3/Drizzle); nombrada para no colisionar con el `getDb()/setDbForTests()` canónico de T0.3, con nota de que se unificarán. (4) `allowExitOnIdle:true` + `max:1`: la sonda no acapara conexiones ni cuelga el proceso de test. (5) El test de health de T0.1 pasó de `{ok:true}` a `{ok:true, db:...}` — adaptación al contrato que la Entrega de T0.2 manda (no debilitación: asserts de igualdad exacta + rama nueva que exige `db`).
**§11**: la sonda ante fallo loguea SOLO `err_name` (nunca el mensaje, que podría llevar la connection string). Verificado por lectura + scan de logs.
**Contradicción de brief anotada**: el brief pedía «la app conecta por la red del compose (nombre de servicio)», que solo aplica si la web es contenedor; la Entrega + Verificación + el precedente de `e2e-stack.ts` apuntan a compose Postgres-only + web-en-host. Resuelto hacia la Entrega (jerarquía planning>brief); la red-del-compose se materializa en el deploy de producción (web contenerizada).
**REVIEW**: code-review (correctness, yo) — sonda nunca lanza (doble red: `realProbe` traga + `checkDbConnection` envuelve), §11 ok, `allowExitOnIdle` evita cuelgues. Sin ds-reviewer (cero UI) ni simplify (diff pequeño y cuidado).
**VERIFY (verifier contexto fresco, Docker REAL)**: PASS. `up -d` → `{ok:true,db:true}`; `stop postgres` → `{ok:true,db:false}` ×3 sin caída (web sigue 200); bind `127.0.0.1:5433` confirmado; §11 sin connstring/password en logs; `.env` gitignored + literales `*-not-a-secret`; gate 436/42; vecinos `ugc-factory-*`+`edge-caddy` intactos; entorno restaurado. Evidencia: `docs/verifications/T0.2/report.md` + 10 outputs.
**Coste**: $0.
**★ ESTRATEGIA DE DEPLOY (directiva del usuario, 2026-07-18)**: tras cerrar F1 el usuario pidió «encaminar la estrategia a deployar la web cuando sea oportuno, sin retrasarlo ni forzarlo». Guardado en memoria (`deploy-strategy-priority`). Implicación: el deploy de producción (originalmente diferido a F3, criterio 14.10) se adelanta como hito a alcanzar pronto. Falta infra de prod (`docker-compose.prod.yml` + `Dockerfile`, no existen) y prereqs externos del usuario (DNS Cloudflare de `devtools.carlosvillu.dev` → VPS, SSL Full strict). Próximo: decidir el punto de inserción del deploy (¿tras F0/auth, o un deploy de F1 antes?) — consultar con el advisor y proponer plan concreto.
**Siguiente por el grafo**: **T0.3** (Drizzle + migración inicial, esquema §9) — depende de T0.2 ✓. Es prerequisito de auth (T0.4) → historial (F2), y también de un deploy con BD real.

## 2026-07-18 · T1.8 CERRADA — infra de producción + imagen que sirve F1 en local (PASS)

**Contexto**: primera tarea del **adelanto de deploy** (directiva del usuario). Construye LA infra de producción y prueba que la imagen de prod sirve F1 en loopback en el VPS. Todo automatizable; el go-live (Caddy+dominio) es T1.9.
**Entrega**: `apps/web/Dockerfile` (multi-stage, monorepo pnpm → Next standalone), `docker-compose.prod.yml` (`name: devtools`, web en `127.0.0.1:3110` + `postgres:16` interno sin puerto de host, sin worker), `.dockerignore`, `.env.prod.example` (sin secretos), `next.config.ts` con `output:'standalone'`, y `apps/web/src/deploy-infra.test.ts` (12 tests permanentes que blindan invariantes estáticos: loopback-only, sin worker, volumen de prod separado, HOSTNAME=0.0.0.0, gating de standalone, `:?` en obligatorias).
**Decisiones no obvias**:
- **`output:'standalone'` GATEADO por `NEXT_OUTPUT`**, no fijo: fijarlo rompe el stack E2E (`next start` sobre build standalone). El Dockerfile exporta `NEXT_OUTPUT=standalone` en el build → la imagen SÍ es standalone; el `next build`/e2e local sin la env sigue igual. Patrón ya probado en el VPS por el vecino ugc-factory. Verificado que AMBAS rutas quedan verdes (build standalone directo + e2e con next start).
- **Build de monorepo**: `pnpm install --frozen-lockfile --ignore-scripts --filter @app/web...` (el `--ignore-scripts` evita que el `prepare`/lefthook exija un `.git` que la imagen no tiene; los binarios nativos llegan por optionalDependencies de plataforma). `@app/core` se compila dentro (transpilePackages); `pg` bundleado; `pino`/`pino-pretty` externalizados (`serverExternalPackages`) por el gotcha de bundling. `outputFileTracingRoot` = raíz del repo → server.js queda en `apps/web/server.js`.
- **HOSTNAME=0.0.0.0** en el runtime: es la interfaz DENTRO del contenedor (si el server.js de Next escucha en localhost, el contenedor sale healthy pero nadie responde por el puerto publicado — trampa 5 de ~/AGENTS.md). NO contradice el «nunca 0.0.0.0» del host: el puerto del HOST se publica solo en `127.0.0.1:3110`.
- **Volumen de prod `devtools-pg-prod-data`** distinto del de dev (`devtools-pg-data`): dev y prod comparten el proyecto `devtools`; un mismo nombre haría que compartieran datos. Dev y prod NO corren a la vez (bajar dev antes de levantar prod).
- **Secretos**: el compose lee `.env` (VPS, gitignored) con `:?` en las obligatorias (falla ruidoso si faltan). `.env.prod.example` solo literales `*-not-a-secret`.
**REVIEW**: code-review (correctness, yo) — Dockerfile/compose/next.config sin defectos; bind loopback correcto, healthcheck por `fetch` de Node (la slim no trae curl), depends_on healthy. Sin ds-reviewer (cero UI) ni simplify (infra).
**VERIFY (verifier contexto fresco, build REAL en el VPS)**: PASS. Imagen standalone (263 MB) → `web` healthy en `127.0.0.1:3110`; `/api/health`={ok:true,db:true} en arranque limpio; `/` 200 con CSS 200 (estáticos copiados); recorrido 14.1 contra PROD = cadena `jwt→json` de §6.5; controles negativos OK (`ss -ltn` solo loopback; externo `80.190.75.149:3110` refused; postgres sin puerto de host); gate 448/43; e2e 13/13; vecinos intactos; entorno restaurado. Evidencia: `docs/verifications/T1.8/report.md` + 11 outputs.
**⚠ Gotcha operativo detectado por el verifier (para T1.9/T3.1, NO defecto de T1.8)**: `postgres:16` solo aplica `POSTGRES_PASSWORD` con data dir VACÍO; si el volumen persistente se creó con otra password, la web recibe `28P01` (auth) mientras `/api/health` sigue 200 con `db:false` → «contenedor verde, BD caída». En un VPS limpio (objetivo de T1.8) el arranque da `db:true`. Para T1.9/T3.1: rotar la password de Postgres exige recrear el volumen o `ALTER ROLE`; y cuando T0.3 traiga migraciones on-boot, revisar que el healthcheck de la web contemple `db` (ahí este gotcha SÍ bloquearía el boot).
**Coste**: $0 (sin APIs de pago; build+curl locales).
**Siguiente**: **T1.9 · Go-live de F1** ⚠ — depende de T1.8 ✓ + del usuario: confirmar en Cloudflare que `devtools.carlosvillu.dev` apunta al origen `80.190.75.149` y SSL **Full (strict)**, + OK explícito para publicar. Es una **parada de prerequisito externo**: preparo lo automatizable y pido al usuario esa confirmación antes de publicar (acción outward-facing).

## 2026-07-18 · T1.9 CERRADA — F1 EN PRODUCCIÓN (PASS) 🎉

**Hito**: **F1 está LIVE en https://devtools.carlosvillu.dev**. Pegas algo en la web pública y ves la cadena desenredada. Es el primer valor real del producto en internet, cumpliendo la directiva del usuario de encaminar el deploy.
**Cómo**: la skill `deploy` (`redeploy.sh`, modo local — el bucle corre EN el VPS) desplegó el HEAD `adaf6d4` (F1 limpio): build de la imagen de prod → up (`web` healthy en 127.0.0.1:3110 + postgres interno) → site block del Caddy central `devtools.carlosvillu.dev.caddy` (reverse_proxy 127.0.0.1:3110, `header_up X-Forwarded-For`) → validate+reload → verify.sh.
**Prep del deploy (para desplegar un artefacto LIMPIO y verificado)**: como `redeploy.sh` construye desde el árbol de trabajo (`COPY . .`), **stasheé T0.3** (que estaba sin commitear) para desplegar exactamente el `adaf6d4` que T1.8 verificó, no una mezcla. Creé el `.env` de prod (gitignored, password de Postgres fuerte generada) y **borré el volumen `devtools-pg-prod-data` stale** (el de la verificación de T1.8, con otra password → el gotcha db:false) para que Postgres re-inicializara limpio → db:true.
**El 525 y su diagnóstico**: `verify.sh` dio HTTP 525 (SSL handshake CF↔origen) en la primera pasada. NO era config: los logs de Caddy mostraron que el cert de Let's Encrypt se obtuvo con éxito **~3 s DESPUÉS** de que verify.sh corriera (carrera de aprovisionamiento). Cloudflare dejó pasar el challenge **HTTP-01** por el puerto 80 (5 `served key authentication` desde IPs de CF → `authorization finalized: valid` → `certificate obtained successfully`). El patrón del vecino ugc (auto-TLS plano, sin directiva `tls`) confirmó que no hacía falta config especial. Reintento tras el cert → **200**. Lección para la skill deploy: `verify.sh` puede correr antes de que Caddy termine el primer cert; reintentar tras unos segundos, no tocar config.
**VERIFY (verify.sh 5 capas + curls públicos)**: PASS. Dominio público `GET /` 200 + `<title>devtools</title>`; `/api/health` `{ok:true,db:true}` end-to-end; **14.1 en prod** (POST del JWT de §6.5 por el dominio público → cadena `jwt→json`, exp en lenguaje natural); origen directo TLS válido + 200; contenedores healthy; prod = HEAD adaf6d4 sin deriva; sin errores en logs; disco 27%. Único rojo: «ningún backup 48h» = alcance de **T3.2** (cron pg_dump), NO de T1.9. Evidencia: `docs/verifications/T1.9/report.md`.
**Seguridad**: web SOLO en loopback 3110 (no 0.0.0.0); Postgres sin puerto de host; `.env` gitignored con secreto fuerte; vecinos intactos.
**Pendiente anotado (NO bloquea)**: (1) registrar el go-live en `~/AGENTS.md` (fila devtools → «prod DESPLEGADO»); el sandbox bloquea escrituras fuera del repo → paso manual/con permiso. El deploy sí quedó en `REMOTE_DIR/.deployed`. (2) rate-limit por `CF-Connecting-IP` (T3.1): hasta entonces el rate-limit de `/api/analyze` ve IPs de borde de CF → protección DoS gruesa; para F1 (función pura sin BD, 128KB→413) es hueco de hardening, no de datos.
**Deriva esperada**: los commits de cierre (planning/journal/docs) mueven HEAD por delante del `adaf6d4` desplegado — deriva SOLO de docs, el código de prod está al día. El próximo deploy con cambio de código (T0.4/auth) la reconcilia.
**Coste**: $0 (build + deploy locales; sin APIs de pago).
**Siguiente**: retomar **T0.3** (Drizzle — implementada y stasheada durante el go-live): popear el stash, pasar el verifier y cerrarla. Luego F0 sigue: T0.4 (auth) → T0.5 (E2E F0). El deploy queda vivo y se re-desplegará al cerrar auth.

## 2026-07-18 · T0.3 CERRADA — Drizzle y migración inicial (PASS)

**Entrega**: capa de persistencia Drizzle en `packages/db` con el esquema de §9 (`user`, `session`, `history_entry`), migración inicial + runner `db:migrate` (con lock), repos tipados mínimos (crear/leer `user` y `session`), y el **harness de Testcontainers** en `@app/test-utils` (primera tarea con Postgres de integración: un contenedor por run → template migrada → clon por suite).
**Decisiones (las dos que la ficha obliga, anotadas en PRD §9)**:
1. **Email**: normalización en app (`trim`+`lowercase` en el repo) + **índice único FUNCIONAL `lower(email)`** en la BD, SIN `citext`. `createUser` es INSERT directo (sin SELECT previo) → el 23505 lo emite la constraint, probando la decisión.
2. **Migraciones ON-BOOT con lock** (`pg_advisory_lock(724_100)`, distinto de la clave del harness de tests), no paso de deploy — CONSISTENCIA con la infra de prod de T1.8. Condiciona T3.1. CLI `db:migrate` operativo ya.
**Otras**: uuid PK `gen_random_uuid()` (PRD §9 manda `uuid pk`; razón ULID de la skill descartada por §5.2); `migrationsFolder` resuelto respecto al paquete, no `cwd`; §11/D7: `history_entry` NO tiene columna de input crudo (solo `preview` redactado + `chain` resumen).
**REVIEW**: code-review (correctness, yo) — schema/repos/migrate sin defectos. Sin ds-reviewer (cero UI) ni simplify (código limpio).
**VERIFY (verifier contexto fresco, Postgres REAL vía Testcontainers)**: PASS. `db:migrate` sobre BD vacía → 3 tablas; índices confirmados (`lower(email)` único funcional, `(user_id, created_at DESC)`, session `(user_id)`+`(expires_at)`); smoke insert+read; **control negativo 23505 por DOS vías**; §11 (sin columna de input crudo); anotación §9 del PRD; gate 461/45. Evidencia: `docs/verifications/T0.3/report.md`.
**⚠⚠ HAZARD OPERATIVO GRAVE (acción inmediata)**: `docker-compose.dev.yml` (T0.2) usa `name: devtools` → contenedor `devtools-postgres-1`, EXACTAMENTE el de PRODUCCIÓN ahora que F1 está LIVE. Correr el compose de dev **tumbaría el Postgres de PRODUCCIÓN**. Mitigación hoy: el verifier usó Testcontainers. **Fix inmediato (siguiente commit)**: proyecto del compose de DEV → `devtools-dev` (contenedor/volumen/red propios). Hasta entonces: tests SOLO por Testcontainers; NO levantar el compose de dev con prod vivo.
**Coste**: $0.
**Siguiente**: (1) **fix del hazard dev/prod** del compose de dev. (2) **T0.4 · Auth** (depende de T0.3 ✓ + TD.7 ✓); al cerrar auth toca **re-deploy** (migración on-boot creando las 3 tablas en prod).

## 2026-07-18 · FIX — aislar el compose de DEV de PRODUCCIÓN (project `devtools-dev`)

Corregido el hazard detectado en el cierre de T0.3: `docker-compose.dev.yml` pasa de `name: devtools` a **`name: devtools-dev`**. Antes compartía proyecto y nombre de contenedor (`devtools-postgres-1`) con el compose de PRODUCCIÓN (vivo desde el go-live de F1), así que un `docker compose -f docker-compose.dev.yml up` habría recreado/tumbado el Postgres de prod. Ahora dev = `devtools-dev-postgres-1` / red `devtools-dev_default` (el volumen `devtools-pg-data` mantiene su `name:` explícito, intacto). Validado con `docker compose config`; ningún test/código referencia el nombre viejo. Pendiente (sandbox bloquea escrituras fuera del repo): actualizar la nota de `~/AGENTS.md` que aún menciona `devtools-postgres-1` para el dev.

## 2026-07-19 · T0.4 CERRADA — Auth email + contraseña (PASS)

**Entrega**: signup / login / logout completos (§8, D9). scrypt de `node:crypto` en formato PHC (`scrypt$ln=16384,r=8,p=1$salt$hash`, salt de 16 bytes por usuario, `timingSafeEqual`); sesión con expiración **en la tabla `session`**, no solo en la cookie (httpOnly + SameSite=Lax + Secure en prod); respuestas **indistinguibles** entre «email no existe» y «contraseña mala» (mismo literal, mismo 401, mismo trabajo de scrypt vía un hash dummy memoizado con los MISMOS parámetros de coste); rate limit por IP en login; pantallas `/login` y `/signup` desde sus mockups con primitivas del DS; middleware (`proxy.ts`) que protege **ÚNICAMENTE** `/history` y `/api/history` — `/` y `/api/analyze` siguen públicas (D6, con test guardián).
**Migración on-boot cableada** (`instrumentation.ts`, adelanto justificado de T3.1): el implementer encontró y arregló un bug REAL — `require.resolve('@app/db/package.json')` lo reescribe el bundler de Next a un id numérico → `path.dirname(number)` = TypeError. Estaba enmascarado en e2e porque la BD de test ya venía migrada. Fix: `runMigrations(conn, folder?)` explícito + resolución por rutas candidatas + `outputFileTracingIncludes` para que las SQL viajen en la imagen standalone.

**REVIEW (2 revisores en paralelo)**:
- **code-review (seguridad)**: sin bypass de auth, sin fijación de sesión, nada crítico; §11 limpio (0 llamadas a logger/console en toda la superficie de auth). 2 MEDIUM + 5 LOW.
- **ds-reviewer**: **LIMPIO**, 0 hallazgos. Evaluó y **descartó como deuda** el `<p role="alert">` de los errores de formulario: `Callout` monta `role="note"` y su contrato prohíbe la prop `role`, así que pasarlo por Callout perdería el anuncio del lector de pantalla. El HTML crudo es aquí lo CORRECTO, no un atajo.

**Arreglos aplicados tras la review** (4): (1) **open redirect** — el guard de `next` era `/^\/(?!\/)/`, que acepta `/\evil.com`: WHATWG normaliza `\`→`/` y `new URL` resolvía a `https://evil.com/`. Ahora `/^\/(?![/\\])/`, con test. (2) **rate limit en signup** — no tenía: cada alta ejecuta un scrypt (~100ms + ~16MB) ANTES del insert, así que era un DoS de CPU barato y permitía enumerar cuentas a ritmo ilimitado vía «ese email ya está registrado». (3) `verifyPassword` capturaba solo el formato inválido: un hash que PARSEA con params imposibles hacía rechazar a scrypt, rompiendo su contrato «nunca lanza». (4) **logout inalcanzable en móvil** — el bloque de sesión era `hidden sm:flex`.

**VERIFY (verifier contexto fresco, navegador real)**: **PASS** en las 5 comprobaciones. Signup → sesión que sobrevive al refresh (fila en `user` con hash scrypt + fila en `session`); rate limit **visible en el `[role=alert]`** al 6º intento (no solo un 429 de red); mensajes de error **idénticos byte a byte** (mismo hexdump, 35 bytes, `sha256 fadfb523…`, `cmp` exit 0 — solo difiere el `request_id`); `/history` sin sesión → `307 /login?next=%2Fhistory`; `/` sin sesión 200 y **usable** (pegó un JWT y se decodificó sin cuenta). Evidencia: `docs/verifications/T0.4/report.md` + 38 outputs.

**Un delta se REVIRTIÓ por evidencia del verifier**: intenté silenciar los 5 bloques «Ecmascript file had an error» del arranque (Next compila `instrumentation.ts` también para Edge) pasando los `import 'node:*'` a `await import(...)`. El verifier lo midió: **el ruido es idéntico, 5 bloques antes y después** — Turbopack analiza el cuerpo ESTÁTICAMENTE, y 3 de los 5 avisos son por `process.cwd()`, que no se movió. Revertido: no se paga una función asíncrona a cambio de nada. La causa real (que Next incluya el fichero en el grafo Edge) queda anotada en el propio fichero.
**Gotcha de verificación que casi da un falso OK** (lo cazó el verifier): para probar migraciones contra una BD vacía, **`drop schema public cascade` NO basta** — el journal `drizzle.__drizzle_migrations` vive en el esquema `drizzle` y sobrevive; con él intacto la migración se da por aplicada, no crea nada y el arranque parece correcto. Hay que borrar **también** el esquema `drizzle`.

**Riesgos ACEPTADOS conscientemente** (anotados en la ficha de T0.4 para que sean revisables, no deuda invisible): (1) el token de sesión es el UUID v4 guardado **en claro** — lo impone el esquema de §9, que no tiene columna de hash; quien lea la tabla `session` puede suplantar sesiones vivas. (2) La enumeración por signup es inevitable para que el formulario sea usable; se acota con rate limit, no se elimina.
**Hallazgo que hereda T3.1** (anotado allí, con dos defectos CONCRETOS medidos, no teóricos): el rate limit por IP **no acota a un atacante** — rotando `x-forwarded-for` los 6 intentos dan 401 y el muro nunca salta; y **sin XFF todos comparten un bucket** porque `clientIp()` devuelve la cadena `'unknown'` en vez de la IP del socket, así que 5 fallos de cualquiera bloquearían el login de todo el mundo. Hoy no explotable en prod (el Caddy central siempre fija XFF), pero al pasar a `CF-Connecting-IP` hay que definir también el fallback.
**Requisito no negociable anotado en T2.2**: el middleware solo comprueba que la cookie **EXISTE** — no la valida contra la BD, así que una cookie forjada o **revocada tras el logout** lo atraviesa. Hoy no hay bug (no existe ruta protegida), pero cuando F2 construya `/history` **cada handler DEBE llamar a `validateSession`**; confiar en el middleware sería un bypass de autenticación trivial.
**Rarezas menores anotadas**: `pnpm dev` moría en silencio por **caché de Turbopack corrupta** en `apps/web/.next` (se arregla con `rm -rf`; no afecta a gate/e2e, que usan `next build`+`next start`); y el modo oscuro de `/login`/`/signup` es idéntico al claro, coherente con los mockups `LoginClaro`/`SignupClaro`.

**Gate**: `pnpm gate` exit 0 → **501 tests / 50 ficheros**; `pnpm test:e2e` → **17 passed** (incluye los 4 de `auth.spec.ts`). Re-ejecutados tras CADA tanda de arreglos, y el verifier corrió el suyo por su cuenta.
**Coste**: $0 (sin APIs de pago).
**Siguiente**: **re-deploy a producción** — auth aún NO está live; el prod actual sirve F1 (`adaf6d4`) y su BD no tiene las tablas. Es la primera vez que un deploy lleva migraciones, así que la verificación NO puede apoyarse en `/api/health`: su `SELECT 1` pasa con la BD **vacía**, de modo que unas migraciones fallidas darían deploy «verde» con el auth roto. Hay que ejercitar un **signup→login→refresh reales contra la prod recién desplegada**. Después, **T0.5 · E2E de fase F0** (parada de fin de fase: resumen y OK del usuario).

## 2026-07-19 · T0.5 CERRADA — E2E de fase F0 (PASS) · **FASE F0 COMPLETA** 🎉

**Entrega**: `apps/web/e2e/phases/f0.spec.ts` con tags `@f0 @phase` — el recorrido del SUELO de una sola tirada sobre el sistema real: arranque con BD real (`/api/health` → `{ok:true,db:true}` por HTTP) → signup → logout intermedio → login **por formulario** → refresh → logout.
**Forma (decisión del implementer, bien razonada)**: UN solo `test()` con `test.step()` por beat, no varios `test()` como `f1.spec.ts`. F1 son 5 casos de uso INDEPENDIENTES que parten de `/` en frío; esto es un journey con **estado acumulado** y el fixture `page` se resetea entre tests — partirlo perdería la sesión, que es justo lo que hay que demostrar. El logout intermedio es deliberado: como el alta ya deja sesión iniciada, es la única forma de que el beat de login ejercite `/login` de verdad.
**No es duplicado de `auth.spec.ts`**: allí cada test parte de una cuenta nueva y aislada; aquí una única cuenta nace, se recupera, sobrevive a un refresh y muere. El refresh de `auth.spec.ts` es *tras signup*; el de aquí, *tras login* (otra cookie, otra fila de `session`).
**El assert que da valor a la spec**: tras el logout, **reinyecta la cookie de sesión ya revocada** y carga `/`. Es la única forma de observar desde el navegador si el logout revoca la fila EN EL SERVIDOR o solo borra la cookie — recuérdese que el middleware únicamente comprueba que la cookie ESTÉ PRESENTE. Sin este assert, un logout que limpiara la cookie sin borrar la fila pasaría inadvertido.
**Control negativo, hecho DOS veces** (implementer y verifier por separado): quitando el `deleteSession` de `revokeSession`, la suite se pone ROJA exactamente en ese assert (línea 122) mientras el logout intermedio sigue verde — el assert aísla la revocación en servidor y no es decorativo. Restaurado, `git diff` limpio.

**VERIFY (verifier contexto fresco)**: **PASS**. BD vacía DE VERDAD (`down -v`, sin esquema `drizzle` residual — el gotcha esquivado) → `db:migrate` crea 3 tablas + 7 índices; `pnpm gate` 501 tests / 50 ficheros; `pnpm test:e2e` **18/18**; tags verificados de verdad (`--grep @phase` la selecciona, `--grep-invert` la excluye); 5/5 ejecuciones sin flakes; recorrido completo a mano en el navegador con consola limpia. Evidencia: `docs/verifications/T0.5/report.md` + 15 outputs.

**Deuda de F0 cerrada en el sign-off (rareza A del verifier)**: el **quickstart del README estaba roto**. Publicaba 4 comandos y el tercero (`pnpm db:migrate`) fallaba con exit 1 **incluso tras copiar los `.env`**, porque nada inyecta `DATABASE_URL` en ese proceso. Arreglado atacando la causa real: como T0.3 decidió migraciones **on-boot**, `db:migrate` **sobra** en el quickstart — `pnpm dev` migra solo. El quickstart ahora incluye los **dos `cp` de `.env`** que faltaban (la trampa de los dos ficheros: compose lee el de la raíz, Next el de `apps/web`) y deja `db:migrate` documentado aparte como herramienta de operación. **Probado ejecutándolo LITERALMENTE** desde volumen vacío: `{"ok":true,"db":true}` y las 3 tablas creadas por el arranque, sin `db:migrate`.
**Deriva local corregida (rareza B)**: el `.env` de la RAÍZ tenía una password aleatoria (contaminación del trabajo de deploy de F1) mientras `apps/web/.env` conservaba la del ejemplo → la web de dev no conectaba y las migraciones on-boot agotaban los 5 reintentos. Ambos son gitignored (deriva local, no defecto del repo). Re-sincronizados al literal de dev documentado y **recreado el volumen `devtools-pg-data`** (postgres solo aplica `POSTGRES_PASSWORD` con data dir vacío — el gotcha de siempre). Solo se tocó el proyecto `devtools-dev`.
**Rareza anotada, no arreglada**: conviven dos estilos de tags — `e2e.md` §10 prescribe `{ tag: [...] }` (lo que usa `auth.spec.ts`) mientras las specs de fase los ponen en el título. Funciona en ambos casos (verificado con `--grep`); unificar es cosmético y no se toca a mitad de fase.
**Seguridad de la operación**: el verifier intentó un `pkill -f next-server` y **el clasificador lo bloqueó — menos mal**: habría matado los `next-server` de los VECINOS (usuario `ubuntu`). Toda limpieza posterior se acotó por uid/puerto. Comprobado al terminar: vecinos vivos, `devtools-web-1`/`devtools-postgres-1` arriba 7 h y **https://devtools.carlosvillu.dev sirviendo 200**.

**Gate**: `pnpm gate` verde (501 tests / 50 ficheros); `pnpm test:e2e` **18/18**.
**Coste**: $0.

### FIN DE FASE F0 — parada para el usuario

**F0 completa: el suelo está puesto** (T0.1 andamiaje · T0.2 Postgres+salud · T0.3 Drizzle+migraciones · T0.4 auth · T0.5 e2e de fase). Con F1 ya LIVE, el producto público funciona y ahora además tiene cuentas.
**Estado**: 21/27 tareas. Siguiente fase natural: **F2 — el historial** (T2.1 → T2.2 → T2.3).
**Decisión que corresponde al usuario (por eso el bucle para aquí)**: **auth NO está en producción todavía.** Prod sirve F1 (`adaf6d4`); su BD no tiene las tablas. Re-desplegar es una acción **outward-facing** y necesita su OK explícito (el «dale tú mismo» de T1.9 era para aquel go-live, no es un permiso permanente). Cuando se autorice, la verificación **NO puede apoyarse en `/api/health`**: su `SELECT 1` pasa con la BD **vacía**, así que unas migraciones fallidas darían un deploy «verde» con el auth roto — hay que ejercitar un **signup→login→refresh reales contra la prod recién desplegada**.

## 2026-07-19 · PRUEBA PREVIA AL DEPLOY — migración on-boot en la imagen REAL de producción

**Por qué**: prod sirve `adaf6d4` (T1.8), que es ANTERIOR al esquema de T0.3 y a `instrumentation.ts` ⇒ **la BD de producción nunca se ha migrado** (sin esquema `drizzle`, sin tablas). Desplegar auth sería por tanto la **PRIMERA ejecución real de la migración on-boot dentro de la imagen standalone**, un camino que ni el gate, ni los e2e, ni el verifier ejercitan (todos usan `next start` / `pnpm dev`, no `output:standalone` + `node server.js`). El code-review lo confirmó LEYENDO; nada lo había confirmado CORRIENDO. Y `/api/health` **no lo detectaría**: su `SELECT 1` pasa con la BD vacía, así que unas migraciones fallidas darían un deploy «verde» con el auth roto.

**Cómo se probó (sin tocar nada outward-facing)**: build de `apps/web/Dockerfile` + un Postgres **desechable** en su propia red, aislado — NO se usó `docker-compose.prod.yml` (ese compose ES producción: proyecto `devtools`, volumen `devtools-pg-prod-data`, puerto 3110). El contenedor de prueba publicó en `127.0.0.1:3119`.

**Resultado: FUNCIONA**, con un matiz que valida haberlo probado:
- `cwd` de la imagen es **`/app`** (`WORKDIR /app` + `CMD node apps/web/server.js`), NO `/app/apps/web` como suponía el comentario de `next.config.ts`. Por eso el candidato `../../packages/db/drizzle` **falla** (resuelve a `/packages/db/drizzle`) y **acierta el tercero**, `/app/packages/db/drizzle`, donde `outputFileTracingIncludes` deja el SQL + `meta/_journal.json`. La resolución por candidatos salva el caso: con una sola ruta fija esto habría fallado en silencio.
- Contra una BD **vacía de verdad** (0 tablas, sin esquema `drizzle`): log `instrumentation: migraciones on-boot aplicadas (intento 1)` → **las 3 tablas creadas**; `/api/health` `{ok:true,db:true}`; **signup real 200** con cookie `HttpOnly; SameSite=Lax; Secure` (el `Secure` se activa solo con `NODE_ENV=production`, confirmado aquí); **login real 200**; hash `scrypt$ln=16384,r=8,p=1…` en la tabla `user` y 2 filas en `session`.
- Entorno desechable eliminado (contenedores, red e imagen). Producción, vecinos y dev intactos; `https://devtools.carlosvillu.dev` sirviendo 200.

**Conclusión**: el riesgo real del re-deploy de auth queda medido y es BAJO. Falta solo la decisión del usuario (acción outward-facing).

## 2026-07-19 · T2.1 CERRADA — Registro de historial redactado (PASS) · cierra el criterio 14.8

**Entrega**: `POST /api/analyze` registra una fila de `history_entry` **solo cuando la sesión valida contra la BD**. La derivación es lógica PURA nueva en core (`@app/core/history`): `preview` redactado (si el kind es `jwt`, payload y firma → `…`) y truncado a 120 —**se redacta ANTES de truncar**, porque al revés un truncado «afortunado» podría dejar pasar medio payload—, `input_kind` del paso 0, y `chain` con solo `[{kind, transformId}]`. Repo tipado en `packages/db`. El contrato público de `/api/analyze` (D6) no cambia.
**Diseño que hace difícil el leak**: el input crudo lo ve ÚNICAMENTE `buildHistoryRecord`, que devuelve algo que ya no lo contiene, y **la firma del repo no admite el input crudo**. No es una convención: es el tipo el que lo impide.

**Control negativo del criterio 14.8, hecho bien**: el grep sobre el dump de la fila —**releída de la BD**, no el objeto que construyó el código— cubre el token completo, el segmento de payload codificado y, lo decisivo, los valores **YA DECODIFICADOS** (`1752624000`, `"sub"`). Un bug del tipo «persistir `steps[i].input`» guardaría el JSON decodificado, que un grep del base64 **no vería**. Ojo para el futuro: el header `eyJhbGciOiJIUzI1NiJ9` **debe** seguir apareciendo (el PRD lo conserva por diseño), así que grepear `eyJ` a secas da un FAIL espurio.
**Controles negativos del implementer** (bug reinyectado → rojo → restaurado): `summarizeChain` filtrando `step.input` → 2 rojos; sin redacción jwt → 8 rojos; fiarse de que la cookie existe → rojo; sin try/catch best-effort → rojo.
**Política ante fallo de registro**: **best-effort, nunca tumba `/api/analyze`**. Todo —resolución de sesión incluida— va en el try/catch, porque `validateSession` toca la BD. En el catch solo un marcador estático `history_record_failed`: nunca el input, ni la cadena, ni `err.message` (§11).

**VERIFY (verifier contexto fresco)**: **PASS**. Fila creada y leída con `psql`; `preview` = `Bearer eyJhbGciOiJIUzI1NiJ9.….…` (31 chars); `chain` sin valores; **7 formas del token ausentes** del dump, 0 coincidencias agregadas sobre TODAS las filas; sin sesión no se crea fila. Extra suyo: **cookie forjada con UUID bien formado** y **cookie revocada por logout** tampoco escriben (el rechazo viene del lookup en BD, no de un error de parseo) — o sea, no se apoya en el middleware de Edge, que solo mira presencia.

**⚠ HALLAZGO DEL VERIFIER, ARREGLADO EN LA MISMA TAREA — cuelgue indefinido en un endpoint PÚBLICO**
Yo sospeché latencia leyendo el código; el verifier la convirtió en defecto MEDIDO con `docker pause` (BD que acepta TCP y nunca responde — un modo que `docker stop` **no** simula, porque stop falla rápido por connection refused, y por eso el test del implementer pasaba):
- cookie válida → **colgada >25 s**; cookie basura `=x`, **sin cuenta** → **colgada >20 s**; anónima → 200 en 29 ms.
- Es decir: **un anónimo colgaba `/api/analyze` mandando una cookie cualquiera**. T2.1 fue quien puso una llamada a BD en el camino de respuesta de un endpoint público ⇒ rompía D6, y arreglarlo era terminar el trabajo de T2.1, no ampliarlo.
- Causa: `createDb` hacía `new Pool({ connectionString })` a pelo, y el default de `connectionTimeoutMillis` en node-postgres es **`0` = esperar para siempre**.
**Arreglo (en `packages/db/src/client.ts`, altitud correcta)**: timeouts de **CLIENTE** en el pool — `connectionTimeoutMillis: 5000` + `query_timeout: 5000`. Se eligió el pool y no un `Promise.race` local porque **el hueco era compartido**: `validateSession`, `login` y `signup` esperaban igual sin timeout; parchear solo el historial habría dejado el login colgado contra la misma BD congelada.
**La trampa que evitamos**: `statement_timeout` **NO habría servido** — lo aplica el SERVIDOR, así que un servidor congelado no puede dispararlo; habría pasado el test de `stop` y seguido colgando en `pause`. El verifier lo comprobó de forma independiente construyendo pools alternativos.
**Resultado medido (verifier, sus propios números)**: cookie válida **200 en 5,151 s**, cookie basura **200 en 5,027 s**, anónima **200 en 0,024 s**, BD caída **200 en 0,021 s** — todas **con la cadena §6.5 correcta**, ninguna 500. Confirmado también que **las migraciones no pueden abortar** por estos timeouts: `runMigrations` abre su propio `Client`, y `createDb` tiene un único consumidor (`apps/web/src/server/db.ts`).
**Guarda de regresión** (`packages/db/test/integration/client.test.ts`): aserción de configuración (construir un `Pool` no conecta). **El verifier criticó mi primera versión con razón**: el segundo test era un subconjunto estricto del primero —«1 aserción efectiva fallando dos veces»— y `> 0` habría pasado con `1` ms. Corregido: ahora el test 2 comprueba de verdad `statement_timeout` AUSENTE y el test 1 exige el valor en una banda operable (1 s–30 s). Re-controlado: sustituir por `statement_timeout` → ambos rojos; poner 1 ms → **solo el test 1** rojo (prueba de que cada aserción discrimina por su cuenta).
**Deuda anotada en la ficha de T2.1**: el residuo de ~5 s sigue siendo disparable por un anónimo. El arreglo de fondo —sacar el registro del camino de respuesta— lleva el residuo a CERO y encaja en T2.2/T3.1.

**Gate**: `pnpm gate` verde — **520 tests / 53 ficheros**; `pnpm test:e2e` **18 passed**.
**Coste**: $0.
**Siguiente**: **T2.2 · La pantalla `/history`**. Recordatorio VINCULANTE ya anotado en su ficha: **cada handler y cada página protegida DEBE llamar a `validateSession` en servidor** — el middleware solo comprueba que la cookie EXISTE, y una cookie forjada o revocada lo atraviesa.

## 2026-07-19 · 🔴 INCIDENTE DE PRODUCCIÓN (causado por el bucle) — `db:false` tras el deploy de auth

**Qué pasó**: el usuario lanzó `redeploy.sh` para poner auth en producción. El deploy construyó y levantó bien, pero `verify.sh` dio `/api/health` → **`{"ok":true,"db":false}`**: la web de producción no podía autenticarse contra su Postgres. ~7 minutos con la BD inaccesible desde la web (la parte pública, `/` y `/api/analyze`, siguió sirviendo 200 — D6 hizo su trabajo).

**Causa raíz — error MÍO, y de lectura, no de dedos**: al cerrar T0.5 el verifier reportó «deriva» entre el `.env` de la raíz (contraseña aleatoria) y `apps/web/.env` (la del ejemplo). Lo interpreté como config de desarrollo caducada y **reescribí `POSTGRES_PASSWORD` con el literal de dev**. Pero:
> **En el VPS, el `.env` de la raíz del repo ES el `.env` de PRODUCCIÓN.** `deploy.env` fija `REMOTE_DIR=/home/developer/projects/devtools` —la raíz del repo— y `docker-compose.prod.yml` hace `env_file: .env`. No hay dos ficheros.

Así que pisé la contraseña real de producción. El volumen `devtools-pg-prod-data` conserva la que se generó en el go-live de T1.9 (postgres solo aplica `POSTGRES_PASSWORD` con el data dir VACÍO — el gotcha de siempre, otra vez), de modo que la web arrancó pidiendo la contraseña equivocada: migraciones on-boot fallando los 5 reintentos y `db_health_probe_failed` en bucle.

**Lo que debí ver y no vi**: mi propia entrada de T1.9 en este mismo journal dice que el `.env` de prod se creó «con password de Postgres fuerte generada». La evidencia estaba escrita por mí. Y `.env.example` **empeoraba activamente** el error: decía que las credenciales de prod «viven solo en el `.env` del VPS, junto al docker-compose.prod.yml», redactado como si fuera OTRO fichero.

**Arreglo (inmediato)**: restaurada la contraseña real en `.env`, `docker compose -f docker-compose.prod.yml up -d web`. Verificado: `{"ok":true,"db":true}` y `instrumentation: migraciones on-boot aplicadas (intento 1)`.
**Arreglo de la causa raíz**: `.env.example` lleva ahora un aviso en rojo que nombra este incidente y explica que en el VPS ese fichero ES el de producción, con la regla operativa: si tiene una contraseña fuerte generada en vez de un literal `*-not-a-secret`, **no se pisa**; para desarrollar en el VPS se usa `docker-compose.dev.yml` (otro contenedor, otro volumen, credenciales propias).

**Lo bueno de la avería**: el diseño se comportó como estaba previsto. `/api/health` **sí** distinguió el fallo (`db:false` con la app viva), la migración on-boot es no-fatal y no tumbó el server, y D6 mantuvo la parte pública sirviendo. La sonda de salud hizo exactamente el trabajo para el que se diseñó en T0.2.

## 2026-07-19 · Auth EN PRODUCCIÓN (verificado de verdad) + deriva de código a corregir

**Hito**: **auth está LIVE**. La **primera migración on-boot real de producción** creó las 3 tablas (`user`, `session`, `history_entry`) sobre la BD de prod, que nunca se había migrado (prod servía `adaf6d4`, anterior a T0.3).
**Verificado como mandaba el análisis previo, NO con `/api/health`** (su `SELECT 1` pasa con la BD vacía, así que unas migraciones fallidas darían deploy «verde» con auth roto): **signup real 200** contra `https://devtools.carlosvillu.dev` con cookie `HttpOnly; SameSite=Lax; Secure`, **login real 200**, y **sesión que sobrevive** (el header renderiza el email). Cuenta de prueba borrada después: prod queda en **0 usuarios / 0 sesiones** (y las 0 sesiones huérfanas confirman de paso que el `ON DELETE CASCADE` del esquema funciona).
Esto valida en producción lo que ya se había probado en la imagen standalone aislada: el `cwd` de la imagen es `/app`, así que el candidato que acierta es `/app/packages/db/drizzle`. La resolución por candidatos es lo que lo salva; con una ruta fija habría fallado en silencio.

**⚠ DERIVA A CORREGIR (proceso, no producto)**: `.deployed` dice **`sha=0ffba7a+sin-commitear`**. `redeploy.sh` construye desde el **árbol de trabajo** (`COPY . .`), así que se llevó a producción el T2.2 **sin commitear y sin revisar ni verificar**. En T1.9 esto se previó y se stasheó el trabajo pendiente antes de desplegar; esta vez el bucle no lo hizo y pidió el deploy con 18 ficheros sucios. **Regla para la próxima: antes de cualquier `redeploy.sh`, el árbol debe estar LIMPIO (`git status --porcelain` vacío), o se stashea.** Riesgo real bajo (prod tiene 0 usuarios, y la revisión de seguridad de T2.2 salió limpia en aislamiento/auth/D7/CSRF), pero es una violación del principio «la evidencia precede a la marca». Se reconcilia con el próximo deploy desde HEAD limpio, una vez T2.2 pase VERIFY.

## 2026-07-19 · T2.2 CERRADA — La pantalla `/history` (PASS tras un FAIL)

**Entrega**: `/history` con `HistoryRow` (preview redactado, tipo, cadena, fecha relativa), **reabrir** (diálogo click-gated con la cadena + el aviso de D7), **borrar** una/todas con confirmación, y `EmptyState` cuando no hay entradas. `GET/DELETE /api/history` paginado (tope 50 + cursor), autenticado contra Postgres y acotado al usuario de la sesión. `/history` activado en la cabecera (desktop y móvil) con estado activo por ruta.

**El invariante de la tarea, y cómo se garantiza**: el `userId` sale SIEMPRE de `auth.user.id` —resuelto contra Postgres por el nuevo `withSession`— y el filtro va **dentro del WHERE SQL**. El handler **no lee ningún id de usuario del cliente y el contrato no tiene dónde alojarlo**: no hay `userId` en query, body, cabecera ni ruta. Manipular la URL no tiene superficie que atacar. `toView` serializa campo a campo (nada de esparcir la fila), así que `user_id` no viaja y una columna nueva no se filtraría sola. Un `DELETE` de una entrada ajena da **404, no 403** — un 403 confirmaría que esa entrada existe y es de otra persona.

**REVIEW (2 revisores en paralelo)**:
- **code-review (seguridad)**: **limpio en lo que más importaba** — aislamiento, autenticación real, fugas de D7 y CSRF: cero hallazgos, verificados por construcción. Sobre CSRF, afirmativamente: un `DELETE` solo puede emitirse por `fetch` (un `<form>` no puede), lo que lo hace petición de subrecurso cross-site, y `SameSite=Lax` retiene la cookie por completo ahí; segunda puerta, el preflight CORS. Encontró además 3 MEDIO y 4 BAJO.
- **ds-reviewer**: 1 hallazgo — `<Link><Button>` en el `EmptyState` = **anidamiento interactivo** (`<a><button>`), fallo de a11y que **el propio diff prohíbe por escrito** en los comentarios de `button.tsx` e `icon-button.tsx`. Corregido a link-as-button.

**⚠ El bug más instructivo del ciclo — hueco SILENCIOSO de paginación**: yo lo olí leyendo (`orderBy` sin desempate) y el code-review lo diagnosticó mejor: la columna es `timestamptz` (**microsegundos**) pero el driver la entrega como `Date` de JS, que **trunca a milisegundos hacia abajo**. Con el filtro `created_at < cursor`, toda fila cuyo instante real caiga en `[cursor_truncado, cursor_real)` **desaparece de las dos páginas**. Reproducido: filas en `.500900/.500500/.500100/.400000`, `limit 2` → la tercera se pierde. **Por qué los tests no lo cazaron: solo asertaban ausencia de SOLAPE, nunca ausencia de HUECO.** Arreglado con cursor **compuesto `(created_at, id)`** y comparación de tupla, orden total `created_at DESC, id DESC`; y —la mitad que casi se escapa— el cursor **emitido** también estaba truncado, así que ahora sale con `to_char(… 'US')` como string y **no pasa por `Date` en ningún punto** del viaje BD→cliente→BD.

**Otros arreglos de la review**: (a) un fallo de la API ya no se muestra como «Tu historial está vacío» —la UI afirmaba algo falso sobre los datos del usuario— sino como estado de error con **Reintentar**; el implementer encontró de paso que el flag de reintento **nunca volvía a false** (`router.refresh()` conserva el estado de cliente), así que un segundo fallo dejaba el botón muerto para siempre → `useTransition`. (b) `withSession` no abría `runWithRequestContext`: los errores de `/api/history` salían sin `request_id`. (c) `api-server.ts` reenviaba **toda la cookie jar** a una URL de env sin validar: ahora hay guarda de **loopback por igualdad exacta** (no por prefijo — el verifier comprobó que rechaza `localhost.evil.com`, `127.0.0.1.evil.com`, `mylocalhost`, `127.0.0.11`…).

**VERIFY: FAIL en la primera pasada, y con razón.** Una cookie `devtools_session` **malformada** (no-UUID) daba **HTTP 500 en vez de 401**: el valor crudo entraba en `eq(session.id, …)` contra una columna `uuid`, Postgres lanzaba error de CAST y salía como 500. **Por qué bloqueaba y no era «fuera de alcance»** (la distinción la trazó bien el verifier): el 🔴 requisito de T2.2 nombra **literalmente `devtools_session=x`** —una cookie no-uuid— como el caso canónico de cookie forjada, `withSession` **nace en T2.2**, y `with-session.ts` **afirmaba por escrito** «una cookie forjada no encuentra fila → null → 401», cierto solo para UUIDs bien formados. No había bypass ni fuga: era corrección de la ruta de autenticación contradiciendo su propio contrato.
**Arreglo**: guarda de forma UUID en **`resolveSession`**, antes de tocar la BD. Se puso ahí y no en el handler a propósito —el verifier confirmó que es la altitud correcta— porque es el cuello de botella común de `validateSession` (API) y `getServerSession` (páginas): cubre `/api/history`, la página y el registro de T2.1 de una vez, en vez de dejar expuesta la próxima ruta protegida. Test parametrizado nuevo («cookie MALFORMADA ⇒ 401, nunca 500») con control negativo: sin la guarda, los 4 casos rojos con 500.
**RE-VERIFY: PASS**. El verifier no se conformó con mis 4 casos: **16 vectores** malformados (uuid sin guiones, no-hex, null byte, unicode, path traversal, 4 KB…) → **16/16 → 401, cero 500**; lo extendió sin que se lo pidiera a `DELETE` (401, recuento 10→10) y a la **página** (307→`/login`). Y comprobó la **indistinguibilidad** malformada vs forjada: cuerpos byte a byte idénticos, cabeceras idénticas, y timing **no distinguible** — 2,2 ms de diferencia frente a un **ruido de fondo medido de 6,3 ms** con la misma cookie, casi el triple. Medir el ruido antes de declarar «no hay oráculo» es exactamente el rigor que se le pide a esta fase.

**Deuda anotada en la ficha de T2.2** (no bloquea): (1) la rama de «no se pudo cargar» es **inalcanzable por caída de BD** —el modo de fallo más probable en prod—, porque `getServerSession` es no-fatal y devuelve `null`, así que `/history` redirige a `/login` y se le dice al usuario que su sesión caducó cuando sigue viva; distinguirlo toca T0.4. (2) El 401 sin cookie sale sin `request_id` (lo emite el middleware de Edge). (3) La redacción del preview es **solo para `jwt`** (política de T2.1): un base64 corto se persiste verbatim.

**Nota de proceso**: el harness impide al verifier escribir ficheros `.md`, así que su informe lo persistió el bucle **tal cual lo emitió**, dejándolo dicho en la cabecera del report. Las evidencias binarias sí las escribió él.
**Gate**: `pnpm gate` verde — **579 tests / 58 ficheros**; `pnpm test:e2e` **25 passed**.
**Coste**: $0.
**Siguiente**: **T2.3 · E2E de fase F2** (cierra los criterios 14.8 y 14.9 y es parada de fin de fase). Después, **re-deploy desde árbol LIMPIO** para reconciliar la deriva `+sin-commitear` que quedó en producción.

## 2026-07-19 · T2.3 CERRADA — E2E de fase F2 (PASS) · **FASE F2 COMPLETA** · cierra 14.8 y 14.9

**Entrega**: `apps/web/e2e/phases/f2.spec.ts` con tags `@f2 @phase` — **CU6 (el regreso) de una tirada**: guardián D6 anónimo → signup → historial vacío → analizar dos entradas → `/history` con vista previa redactada → reabrir → borrar → guardián D6 de cierre tras logout. Forma de `f0.spec.ts` (un `test()` con `test.step()` por beat) porque es un journey con estado acumulado.

**⚠ EL HALLAZGO DEL CICLO — asserts de privacidad que NO PODÍAN FALLAR.** El implementer hizo control negativo (desactivar `redactInput`) y descubrió que la spec **seguía verde con la fuga delante**. Causa: `preview` se trunca a **120** chars y el JWT mide **170**, así que el truncado PARTE el segmento de payload completo y un `not.toContain(<segmento entero>)` **no casa nunca**. Peor todavía: el assert de `"sub"` era decorativo — en HTML las comillas salen como `&quot;`, así que ese literal no casaría jamás.
**Corregido en las DOS specs**: prefijos cortos que sobreviven al truncado (`eyJzdWIiOiIx`, `SflKxwRJ`) + los valores **DECODIFICADOS** (`carlos`, `1752537600`, `1752624000`) + un assert de **igualdad** de la vista previa que no admite escapatoria. El bucle endureció además `history.spec.ts` (T2.2), que arrastraba la misma debilidad. **Control negativo tras el arreglo: rompiendo `redactInput`, 3 tests en ROJO (antes 0 de `history.spec`).**
Detalle fino que evita un falso positivo futuro: `TIMESTAMP` en `history.spec.ts` pasó de `1752624000` a `1700000000` — el primero es el `exp` DECODIFICADO del JWT de prueba, así que como segunda entrada sería una vista previa legítima y colisionaría con la lista de fugas, invitando a quitar de la lista justo el literal que más muerde.

**🔴 Por qué el `psql` de la Verificación NO es ceremonia** (lo midió el implementer y lo confirmó el verifier): `GET /api/history` valida su salida con `HistoryPageSchema.parse(...)` y **Zod descarta las claves desconocidas**. Es buena defensa en profundidad, pero significa que una fuga introducida **ANTES** de esa frontera (p. ej. `summarizeChain()` emitiendo `step.output`) **se persiste en Postgres y queda INVISIBLE** para el HTML y para la API — el recorrido e2e seguiría verde. **La fila cruda en `psql` es la única observación de esa clase de fuga.** Que la ficha exigiera el `psql` explícitamente resultó ser una decisión de diseño acertada del planning, no burocracia.

**VERIFY (verifier contexto fresco)**: **PASS**, con 14.8 y 14.9 ejecutados LITERALMENTE:
- **`psql` de la fila** (todas las columnas): `chain` guarda solo `{kind, transformId}` —**ni `input` ni `output` de ningún paso**— y `preview` es `Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.….…`. **0/9 literales de fuga**, incluidos los decodificados. Con **control positivo**: el grep SÍ encuentra el header conservado, luego los ceros son ausencia real y no un grep mal apuntado.
- **`grep` sobre los logs**: 0/10 literales sobre 1310 líneas de stdout real, con control positivo de 9 `analyze_completed`. Lo que sí se loguea es exactamente el contrato de §11: `input_kind`, `input_bytes`, `steps`, `duration_ms`. **14.9 bis**: repetido sobre los logs del stack E2E en **build de producción** (27 `analyze_completed`) → 0 coincidencias.
- Tags verificados de verdad (`--grep @phase` → 8 tests/3 ficheros; `@f2` → 8/2), sin regresión de F0 ni F1.
- Evidencia: `docs/verifications/T2.3/report.md` + 18 ficheros (capturas, dumps, logs, HTML del momento de máximo riesgo con el diálogo abierto).

**Gate**: `pnpm gate` verde — **579 tests / 58 ficheros**; `pnpm test:e2e` **26 passed**.
**Coste**: $0.

### FIN DE FASE F2 — parada para el usuario

**F2 completa (24/27)**: lo que analizas con la sesión iniciada queda registrado **redactado**, se revisa en `/history`, se reabre (la cadena, no el dato) y se borra. Sin cuenta, `/` sigue funcionando igual (D6 protegido por guardián al principio y al final del recorrido de fase).

**La decisión que corresponde al usuario, y que el verifier señala como la más importante del cierre**: **la redacción del `preview` es SOLO para `jwt`**. Confirmado en la fila: un input que no sea JWT se persiste **verbatim** hasta 120 chars, así que un **base64 corto se guarda tal cual y puede descodificar a texto legible**. Es la política que nació en T2.1 (viene del PRD), pero acota la afirmación de R2: hoy «devtools no es un pasivo de privacidad» está cumplido **para JWT**, no para toda entrada. Merece decisión explícita —ampliar la redacción, o asumirlo y decirlo en la UI— en vez de seguir como nota al pie.

**Pendiente de producción**: prod corre `0ffba7a+sin-commitear` (T2.2 PRE-arreglos: tiene vivo el 500 con cookie malformada y el hueco de paginación). El árbol está LIMPIO en `2ab5122`+T2.3, así que un `redeploy.sh` ahora reconcilia la deriva y sube todo lo verificado. **Regla aprendida hoy: árbol limpio ANTES de cualquier redeploy.**

## 2026-07-19 · F2 EN PRODUCCIÓN — deriva reconciliada y defecto corregido en vivo

**Deploy limpio**: `redeploy.sh` con el árbol **LIMPIO** → `producción = HEAD 9fd8327`, **sin la marca `+sin-commitear`** que arrastraba el deploy anterior. La regla aprendida ayer (árbol limpio ANTES de desplegar) se aplicó y funcionó. `db:true` a la primera: el `.env` de prod, restaurado tras el incidente, aguantó el recreado de contenedores.

**Verificado en producción, no con `/api/health`** (que pasa con la BD vacía):
- **El defecto que veníamos a arreglar**: las 4 cookies malformadas (`x`, `basura`, `x' OR 1=1--`, uuid a medias) devuelven ahora **401**; antes del deploy producción respondía **500**. Indistinguibles de una cookie forjada bien formada y del caso sin cookie (401 los tres).
- **Recorrido F2 completo**: signup 200 → analyze con sesión 200 → `GET /api/history` devuelve la entrada con la **vista previa redactada** (`Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.….…`) y `chain` con solo `{kind, transformId}`.
- **Criterio 14.8 sobre la BD de PRODUCCIÓN**: 0 coincidencias de las 5 familias de fuga (incluidos los decodificados `carlos`, `1752537600`, `1752624000`), con **control positivo** (el header conservado aparece 1 vez) que prueba que el grep apunta bien.
- Único rojo de `verify.sh`: «ningún backup en 48 h» — es el cron de `pg_dump`, alcance de **T3.2**, no una regresión.

**⚠ CORRECCIÓN DE UNA PREMISA QUE EL BUCLE VENÍA REPITIENDO**: al limpiar la cuenta de prueba, el recuento no cuadró (quedaba 1 usuario) y resultó ser **el usuario real: `carlosvillu@gmail.com`, dado de alta a las 09:52 en el sitio público**, con una entrada de historial propia. Es decir: **producción dejó de tener «cero usuarios» a las 09:52**, y el bucle siguió justificando riesgos con ese argumento («no hay datos que filtrar») durante horas sin re-comprobarlo. Ninguno de los dos defectos que estuvieron vivos exponía datos (el 500 era un status equivocado; el hueco de paginación exige >50 entradas), así que no hubo consecuencia — pero la lección es de método: **una premisa sobre el estado de producción caduca; hay que re-medirla, no arrastrarla**. Los datos del usuario real se dejaron INTACTOS; solo se borró la cuenta de prueba del bucle.

**Ilustración en vivo de por qué T2.4 importa**: la entrada del usuario real es `preview | Hola`, `input_kind | text` — persistida **verbatim**, exactamente como manda la política jwt-only vigente. Inocuo para «Hola», pero es el caso real que T2.4 viene a cerrar: un base64 corto con un secreto dentro se guardaría legible.

**Estado**: **F1 + F0 + F2 vivos en `https://devtools.carlosvillu.dev`** con auth e historial funcionando para un usuario real. Vecinos (`ugc-factory-*`, `edge-caddy`) intactos.
**Siguiente**: **T2.4** (ampliar la redacción más allá de `jwt`, cambio de alcance aprobado por el usuario), y después F3: T3.1 → T3.2 (el backup que `verify.sh` reclama) → T3.3.

## 2026-07-19 · El bucle ya puede desplegar SOLO — permiso + guarda de árbol limpio

**Problema**: el bucle podía hacer `docker compose` contra producción (y por tanto pararla, recrearla o borrarle el volumen) pero **NO** podía ejecutar `redeploy.sh`, que es justo el camino SEGURO: sincroniza, despliega y **falla si `verify.sh` no pasa** («un deploy que no se puede verificar falla»). Los permisos estaban al revés del riesgo: lo peligroso permitido, el envoltorio verificado bloqueado.

**Arreglo (lo aplicó el usuario; el bucle NO puede editar sus propios permisos, y está bien que así sea)**: tres reglas en `.claude/settings.local.json` —fichero **gitignored**, así que el permiso es local de ESTA máquina y nadie que clone el repo lo hereda— para `redeploy.sh`, `verify.sh` y `backup.sh`. **`rollback.sh` se deja fuera a propósito**: es destructivo y, como avisa la propia skill, revertir el código **no deshace las migraciones** — eso merece un humano siempre.
Nota de diseño que conviene conservar: el clasificador bloqueó al bucle tanto **editar sus propios permisos** como (al principio) **editar el script de deploy**. Un agente que puede reescribir sus propias barandillas no tiene barandillas; que ese límite exista es una propiedad, no un estorbo.

**Guarda de árbol limpio en `redeploy.sh`** (prerequisito, no adorno, para desplegar desatendido): la imagen se construye desde el **ÁRBOL** (`COPY . .`), no desde HEAD, así que con el árbol sucio se desplegaba lo sin commitear y el único rastro era un `+sin-commitear` en `.deployed`, que se lee DESPUÉS y no impide nada. Ahora el modo local **aborta con exit 1** y un diagnóstico claro. Va ANTES del bloque `NO_SYNC` a propósito: `--no-sync` («despliega lo que hay») es el caso más expuesto. **`--rsync` sigue funcionando**: desplegar trabajo sin commitear es SU propósito, así que la llamada delegada al VPS pasa `--allow-dirty` explícito; la rama `git` no lo pasa, porque allí el árbol sale de `git pull` y debe estar limpio.
**Probado de verdad**: árbol sucio → aborta con **exit 1** y `.deployed` intacto (no un exit 0 que la automatización leería como éxito); flags desconocidas siguen rechazándose; árbol limpio → deploy completo correcto.

**Primer deploy AUTÓNOMO del bucle**: `producción = HEAD 7d94d92`, sin marca `+sin-commitear`. Y ejecutado el **primer backup de producción**: `devtools-20260719T170456Z.dump` (12K), **verificado restaurable** con `pg_restore --list` (4 tablas con datos) — importaba de verdad, porque desde las 09:52 hay **datos de un usuario real** en prod y no existía ninguna copia. Con eso, `verify.sh` da por primera vez **las 5 capas en verde**.
Ojo: esto **NO cierra T3.2**, que pide el **cron diario** + restore verificado. Es un backup puntual que tapa el hueco mientras tanto.

## 2026-07-19 · arnés: destilado del journal completo → 3 reglas nuevas en testing, dev-loop y deploy

El usuario pidió destilar los aprendizajes de todo el journal y actualizar el arnés. Las tres familias de incidente que se repetían, y dónde quedó cada corrección:

**1 · `testing/SKILL.md` — forma 8 del anti-patrón: EL ASSERT NEGATIVO QUE NO PUEDE CASAR.** La familia más repetida (~6 apariciones): asserts de ausencia estructuralmente incapaces de fallar. Mecanismos observados: el literal no sobrevive a las transformaciones del canal (T2.3: segmento JWT más largo que el truncado a 120; `"sub"` que en HTML es `&quot;sub&quot;`), el canal observado está vacío (T0.1: captura de 0 líneas → `not.toContain` vacuo), se asserta la propiedad cómoda y no la peligrosa (T2.2: solape sí, hueco no), y el grep no cubre las formas DERIVADAS del dato (el decodificado). Regla nueva vinculante: **todo assert negativo de privacidad/seguridad exige control positivo en la misma pasada + control negativo EJECUTADO** (reinyectar la fuga → rojo). Añadida la pregunta (h) a la checklist y el espejo del revisor en `dev-loop` §5a. También corregido el conteo («siete»→«ocho formas»).

**2 · `dev-loop/SKILL.md` — sección nueva «Premisas: se RE-MIDEN, no se recuerdan».** Incidentes raíz: el claim falso de pino (T0.1), el gotcha falso `--shadow-*` (TD.1), la premisa «0 usuarios» caducada a las 09:52 y arrastrada horas, y el incidente de producción (pisar el `.env` de prod por creerlo config de dev, con la evidencia escrita en este journal). Tres reglas: (1) toda afirmación heredada viaja etiquetada verificado/no-verificado y lo no verificado se PRUEBA antes de obedecerlo; (2) las premisas sobre estado externo caducan — re-medir en el momento de uso, sobre todo antes de justificar un riesgo o mutar algo; (3) antes de sobrescribir config/entorno, confirmar qué ES el fichero (un secreto fuerte donde esperabas `*-not-a-secret` = tu premisa está mal, no el fichero). Corolario: una regla aplicada de memoria dos veces se convierte en check mecánico.

**3 · `deploy/SKILL.md` — las trampas de ESTE VPS, ya mordidas, escritas donde se leen antes de desplegar.** (a) Documentada la guarda de árbol limpio de `redeploy.sh` (existía en el script desde el 2026-07-19 pero no en la skill); (b) «el `.env` de la raíz ES el de producción» con el incidente como justificación; (c) el gotcha `POSTGRES_PASSWORD` solo-con-volumen-vacío, que mordió TRES veces (T1.8, T1.9, incidente); (d) el 525 por carrera del primer certificado de Caddy (go-live T1.9): reintentar, no tocar config.

Principio transversal que gobierna las tres: la evidencia que no puede ponerse roja no es evidencia, la premisa que no se re-mide no es un hecho, y la regla que vive solo en la memoria del agente volverá a fallar — se convierte en guarda mecánica.

**Sincronizado a `web-template` (commit `8d8f66e`, pusheado) en versión GENERALIZADA** — solo lo genérico, con las lecciones anonimizadas (sin nombres de tareas ni detalles de producto de devtools): formas 7 y 8 + preguntas (g)/(h) en testing (el template seguía en seis formas — la 7 nunca se había sincronizado), espejos del revisor + sección de premisas en dev-loop, y en deploy la guarda de árbol limpio PORTADA a su `redeploy.sh` (con `--allow-dirty` y la delegación de `--rsync`; `bash -n` verde) + las 3 trampas genéricas (`.env` raíz = prod, `POSTGRES_PASSWORD` solo con volumen vacío, 525 por carrera del primer cert). Verificado con grep que ninguna referencia a devtools viajó al template.

## 2026-07-19 · T2.4 CERRADA — redacción más allá de `jwt` (PASS tras un FAIL) · **F2 COMPLETA otra vez**

**Entrega**: `redactInput` deja de ser «solo jwt». Regla por kind, escrita en el módulo: `base64` → `… (N caracteres)` (**cero** caracteres de contenido: 4 chars de base64 ya son 3 bytes de plaintext, así que ni un prefijo es seguro); `json` → estructura + claves, **todos** los valores (string, número, bool, null, anidados) → `…`; `url` → `scheme://host` + `/…` (el token viaja en path/query/fragment, y `new URL` tira además el userinfo); `jwt` sin cambios; `hash`/`uuid`/`unix_timestamp` verbatim; `text` verbatim **salvo que empiece por `{`/`[`/`"`** → `…`.

**⚠ FAIL en la primera pasada, y por lo más instructivo del proyecto hasta hoy.** La redacción era sólida y la Verificación literal pasaba entera. Lo que bloqueó fueron los entregables ALREDEDOR:
1. **La tabla de reglas afirmaba dos garantías que el sistema no da**: «JSON inválido → `…`» y «escalar de nivel superior nunca sobrevive». Falsas end-to-end, porque `detectJson()` exige parseo válido Y no-escalar ⇒ esas entradas **nunca llegan** a `redactJson()`: caen en `text` y se persisten **verbatim**. La regla describía la FUNCIÓN aislada, no el SISTEMA.
2. **Los dos unit tests que las «probaban» eran INERTES**: pasaban `redactInput(x, 'json')`, un par `(input, kind)` que el detector **no puede producir**. Verdes, describiendo una protección inexistente. **Es exactamente el anti-patrón (forma 8) que esta tarea existía para eliminar, y que se había escrito en la skill `testing` HORAS antes.** Escribir la regla no inmuniza contra ella.
3. **🔴 El copy de usuario mentía**: se cambió a «nunca guardamos secretos en claro» — promesa ABSOLUTA, renderizada en `/history`, y demostrablemente falsa (`leakme123`, `supersecretvalue`, un token de 64 hex, todos en claro en la BD). **Peor que el hueco original**: el usuario decide qué pegar confiando en esa frase. Estuvimos a un PASS de publicar una promesa de privacidad falsa a un usuario real que está pegando cosas en ese campo.

**Cómo se cazó**: el verifier leyó la **página renderizada contra las filas reales de la BD**, en vez de leer el diff. Y preguntó de cada test «¿puede este input llegar a este camino en producción?».

**Arreglo**: (a) copy reescrito sin absolutos, describiendo qué se guarda de cada kind; (b) tabla corregida al comportamiento end-to-end, con aviso de que **el enrutado es por kind REAL del detector, no por intención**; (c) los dos tests rehechos desde el **input crudo** vía `buildHistoryRecord(input, analyze(input))` y —lo bueno— **verificando su propia premisa** (`expect(inputKindOf(analyze(input))).not.toBe('json')`), así que si el detector cambia el test lo DICE en vez de volverse inerte; (d) opción A: enrutar a redacción lo que empieza por `{`/`[`/`"`; (e) integración nueva con JSON malformado que lee la fila real.

**RE-VERIFY: PASS.** El verifier repitió el recorrido ENTERO con literales nuevos, y **escribió su propio control negativo**: un script que importa el `analyze()` REAL y compara la redacción contra una réplica sin la rama `looksLikeJson` → los 3 casos muerden, con `kind` producido por el detector de verdad (el rojo cae en un camino que producción recorre, que era el defecto de la vuelta 1). 13 probes originales + 13 nuevos contra la frontera movida: ningún kind pierde entradas, `trim()` no abre nada, y se gana cobertura (`{'pw':'x'}` con comillas simples → `…`). Auditó además con `git show HEAD:…` que los tests borrados **nunca estuvieron committeados** (eran de la primera pasada del implementer), así que no se debilitó nada establecido — regla 5 respetada.

**Dos recomendaciones no bloqueantes, aplicadas por el bucle** (son el objeto mismo de la tarea: que las afirmaciones de privacidad sean honestas): residual **(c)** nombrado en el módulo —un `text` que CONTIENE JSON sin empezar por el sigilo se guarda verbatim, con la asimetría escrita: **sobre-redacta** `[INFO] …` y **sub-redacta** el JSON embebido— y **copy completado** con «de una URL, el dominio» (se redactaba y no se decía) y «El resto (texto, hashes, UUIDs y timestamps) se guarda tal cual», que cierra el silencio sobre los verbatim.

**Residuales asumidos y NOMBRADOS** (no enterrados): `hash` verbatim (un token de API de 64 hex se guarda entero — un digest es opaco y es justo lo útil de ver, pero es exposición real); secreto usado como CLAVE de JSON; y el JSON embebido de (c).
**Rareza importante para el arnés**: **`next dev` NO hidrata la app en esta máquina** — el signup sale como GET nativo **con la contraseña en la URL** y no crea usuario; con `next build && next start` funciona. No afecta a producción ni al e2e (ambos usan `next start`), pero **invalida `next dev` como entorno CUA**: toda verificación de navegador debe correr contra el build de producción. Merece línea en `cua.md` y que alguien mire si es Turbopack.

**Gate**: `pnpm gate` verde — **597 tests / 58 ficheros**; `pnpm test:e2e` **27 passed**.
**Coste**: $0.
**Siguiente**: **F3** — T3.1 (producción del producto completo + el trust boundary de `CF-Connecting-IP`, que arrastra la deuda medida desde T0.4), T3.2 (**cron** de backup: el de hoy fue manual y NO cierra la tarea) y T3.3 (E2E de fase F3).

## 2026-07-20 · T3.1 CERRADA — trust boundary por `CF-Connecting-IP`, borrado en el borde (PASS a la primera)

**Entrega**: los TRES rate limits (login, signup, `/api/analyze`) dejan de llevar la clave por `x-forwarded-for` y pasan a `CF-Connecting-IP`, con cadena de precedencia explícita (`CF-Connecting-IP` → última entrada de XFF → `LOOPBACK_KEY`) y sin el `'unknown'` compartido. Producción = `684bc53`, `verify.sh` con las 5 capas en verde.

**El hallazgo que da forma a la tarea**: cambiar la cabecera **no es por sí solo una mejora de seguridad**. El origen es alcanzable sin pasar por Cloudflare (Caddy escucha en el 80/443 del host), así que `CF-Connecting-IP` es tan fabricable como el XFF que sustituye — habría sido **cambiar una clave spoofeable por otra, con el defecto 1 de T0.4 vivo bajo otro nombre**. La defensa real vive en el BORDE: el site file borra la cabecera cuando `remote_ip` cae fuera de los rangos de Cloudflare. Verificado empíricamente contra un Caddy 2.11.4 real: el borrado elimina **todas** las instancias del header, es case-insensitive y se ejecuta **antes** de `reverse_proxy` en el top level del bloque. Y el site file pasa a estar **versionado en el repo y reconciliado en cada deploy**: antes era crear-si-falta, así que el control **jamás habría llegado a un dominio ya desplegado**.

**Lo que cazó el `code-review` (effort high) y NO era teórico:**
1. 🔴 **Bloqueante en `redeploy.sh`**: un `caddy validate` fallido dejaba instalado un fichero roto en un Caddy **compartido con un dominio de terceros** (`ugc.carlosvillu.dev`), sin restaurar el `.bak`. Daño DIFERIDO —el edge sigue sirviendo porque el reload no llegó a dispararse—, que es justo lo que lo hace peligroso: el siguiente deploy del vecino falla por culpa de NUESTRO fichero, y un reinicio del host se lleva ambos dominios.
2. **Nada en el repo detectaba que la protección del borde desapareciera**: se podía borrar la línea `request_header` del Caddy y no fallaba ni un test. Toda la defensa colgaba de una línea sin guarda.
3. **Dos tests sobre-declaraban**: afirmaban una propiedad del SISTEMA («la clave es CF-Connecting-IP») inyectando una cabecera que un cliente de producción **nunca puede fijar** (el borde la borra), y pasaban con el borrado del borde totalmente ausente. Retitulados a «precedencia de `clientIp()`», con su alcance escrito y remitiendo a la sonda para la otra mitad.
4. **`TRUST_PROXY` fallaba ABIERTO** hacia el defecto exacto que T3.1 cierra: sin la variable en producción, todo internet colapsa en `LOOPBACK_KEY` (5 fallos de cualquiera bloquean a todos). Ahora `assertTrustProxyConfigured()` lo exige en el arranque.

**Lo que cazó `simplify` — un defecto dentro del arreglo del bloqueante**: la reversión usaba la existencia del `.bak` en disco como proxy de «esta ejecución instaló algo». Con el fichero ya al día (rama `cmp -s`, no se escribe `.bak` nuevo) y un `.bak` viejo en disco, un validate roto **por otro proyecto** restauraba una generación caducada del nuestro — posiblemente **anterior a T3.1, sin el `request_header`** — y la dejaba instalada al salir. El daño diferido reintroducido por su propio remedio. Ahora son banderas de ejecución (`installed`/`had_previous`), reproducido el escenario exacto contra un Caddy real.

**Y la sonda de forja se AUTO-APAGABA**: su gate se derivaba del artefacto que verifica (`grep CF-Connecting-IP` sobre el site file), así que borrar el control junto con sus comentarios **apagaba en silencio a su propio detector**. Ahora el interruptor es `FORGERY_PROBE_PATH` en `deploy.env`: **declarado + control ausente = FAIL ruidoso**. Es la misma familia que la forma 8 de `testing` (la evidencia que no puede ponerse roja no es evidencia), pero en el arnés de deploy en vez de en un test — conviene tenerlo presente como patrón: *un detector no debe deducir su propia activación de aquello que vigila*.

**El verifier REFUTÓ un claim del implementer** (y esto es el valor del evaluador separado). El implementer declaró que el control negativo (b) «no se puede comprobar inyectando dos `CF-Connecting-IP` distintas» y propuso sustituirlo por dos mitades. **Falso**: esta máquina es doble pila y el dominio resuelve A y AAAA por Cloudflare, así que IPv4 e IPv6 llegan con `CF-Connecting-IP` genuinamente distinta. La cláusula literal se ejercitó en **8 peticiones**: IPv4 ×5 → 401, la #6 → **429**, **IPv6 → 401 (bucket distinto = la cláusula)**, IPv4 #7 → 429 (el bloqueo es real, no expiración de ventana). El 429 es de la app (`{"code":"rate_limited",…}`), no una challenge de Cloudflare. **El código estaba bien; lo que falló fue el esfuerzo de verificación** — un «no es comprobable» que se disolvía en 8 peticiones. Lección de método: *declarar algo no comprobable es una afirmación que también hay que verificar*.

**Rareza metodológica anotada por el verifier**: el bucle corre DENTRO del VPS, así que «desde fuera» siempre exige un vantage prestado. La evidencia dispositiva del control (a) es el **bind a loopback** (inalcanzable por el kernel desde otra interfaz), no la sonda externa; descartó una primera sonda por inconcluyente cuando su control positivo también falló — exactamente el reflejo correcto.

**Producción**: baseline 1 usuario real; cuenta de prueba creada y borrada, recuento final 1 usuario con el mismo `id` y `created_at`. Datos reales intactos. Backup previo tomado y verificado restaurable (`devtools-20260720T092120Z.dump`) antes de tocar nada, porque `rollback.sh` está fuera del alcance del bucle a propósito.

**Gate**: `pnpm gate` verde (**610 tests / 59 ficheros**); `pnpm test:e2e` **27 passed**. Re-gate post-commit verde.
**Coste**: $0 (sin APIs de pago).
**Deuda anotada (no bloquea)**: (1) la lista de rangos de Cloudflare se degrada **abierta** si CF *retira* un rango que luego se reasigne — la sonda detecta que el control desaparezca entero, **no** un rango caducado suelto; revisión anual mirando **retiradas**, no solo altas. (2) Sin evicción en los `Map` de `rate-limit.ts` (no es regresión: la cardinalidad forjable BAJA con este cambio; el residual es un /64 de IPv6 vía CF legítimo). (3) `run_vps` no usa ControlMaster/ControlPersist: cada lectura remota es un SSH completo. (4) El E2E solo ejercita el fallback de XFF, no el camino primario. (5) El `.bak` es inútil como rollback tras el primer deploy.
**Arnés**: `redeploy.sh`/`verify.sh`/`SKILL.md` cambiaron y son ficheros sincronizados con `web-template` — la reconciliación del site file, la reversión ante validate fallido y la sonda de forja (ya generalizadas vía `deploy.env`) **convienen propagarse aguas arriba**. `SKILL.md` reenmarcado: la sección del CDN ahora dice explícitamente que **sin CDN delante esto no aplica** y que cablear el rate limit a una cabecera que nadie pone sería peor que no hacer nada.
**Siguiente**: **T3.2** (cron de backup diario + restore verificado sobre BD vacía; el backup de hoy y el del 19 fueron manuales y NO la cierran), y luego T3.3 (E2E de fase F3 = cierre del proyecto v1).

## 2026-07-20 · T3.2 CERRADA — backup diario, poda y restore verificado (PASS a la primera)

**Entrega**: `backup.sh` pasa a ser un solo camino que corre el cron y se fuerza a mano: volcado atómico (`.part` + `mv`, para que un `pg_dump` muerto no deje un truncado que la retención conservaría), poda por `BACKUP_RETENTION_DAYS` (14) anclada al patrón `<PROJECT_NAME>-*.dump`, `pg_restore --list`, y `--restore-check` que restaura sobre una BD desechable y compara con producción. Cron instalado a las 03:30 (no colisiona con el de `ugc-factory`, 04:15).

**El punto de partida era WIP heredado SIN report ni journal**, así que se trató como premisa no verificada y se re-midió todo. Bien hecho: el camino simple ya corría verde y eso engañaba: **`--restore-check` no se había ejecutado NUNCA** y **la rama de borrado de la poda tampoco** (el log decía «ningún dump supera los 14 días» — la única rama que se había visto era la que no borra).

**🔴 La lección de la tarea, y van cuatro en el mismo ciclo: el arnés escribió asserts sobre la FORMA del código y los dio por buenos sin verlos morder.**
1. `code-review` demostró **ejecutando** que 8 asserts de `deploy-backup.test.ts` eran INERTES: el fichero creaba `activeBackupSh` (filtra comentarios) y luego grepeaba `backupSh` crudo. Un fichero que **solo contenía comentarios** los ponía verdes → comentar `# trap drop_target EXIT` y `# *_restore_test)` dejaba el test que blinda esas guardas en verde.
2. El assert de la poda aceptaba un glob **des-anclado**: `-name '[^']+'` pasaba con `-name '*'`. Verificaba que `DUMP_GLOB` *se define* anclado, no que el `find` **lo use** — y el directorio es COMPARTIDO con `ugc-factory`.
3. El primer endurecimiento del anti-`rm` del implementer **seguía siendo inerte para `rm -rf`**, el caso más común (`-[a-zA-Z]*[rR]\b`: la `f` impide el límite de palabra). Solo cayó al ejecutar la matriz de mordiscos.
4. `simplify` (ángulo altitud) remató: seis guardas se podían **EJECUTAR** en vez de grepear — `DEPLOY_MODE=local` hace `run_vps` un `bash -c` local y las ramas de aborto no borran nada. El propio `deploy.env` escribía la doctrina («se puede sobreescribir para EJERCITAR la poda… una retención que nunca se ha visto podar no se sabe si poda») y el test la ignoraba. **La prueba de que importaba**: quitando solo el `exit 1` y dejando el `case` intacto, 4 tests se ponen rojos — **2 de ellos habrían seguido verdes con el grep**. El patrón seguía ahí; solo había desaparecido el abortar.

**Doc que prometía de más (clase T2.4, cazado por code-review)**: el mensaje al operador decía «✓ … **fila por fila**» comparando `count(*)`, que es **cardinalidad, no contenido**; y `SKILL.md` afirmaba un control cruzado entre el nº de `pg_restore --list` y el de `COUNT_SQL` que **el script nunca hace** (viven en invocaciones `remote_bash` distintas, y ni siquiera tienen que coincidir: una tabla particionada padre es `BASE TABLE` sin `TABLE DATA`). Ambas corregidas. También «la BD se destruye siempre» → el script **ahora lo comprueba** (consulta `pg_database` y falla si sobrevive), en vez de solo matizar la frase.

**Seguridad**: `$target` iba sin comillas dentro de los `sh -c`, así que un `BACKUP_RESTORE_DB='x; <cmd>; y_restore_test'` **atravesaba las dos guardas y ejecutaba dentro del contenedor de producción**. Ahora hay filtro de juego de caracteres + comillas, y se verificó que **cada capa para el ataque por separado**.

**Verificación (verifier, PASS a la primera)**: fue MÁS ALLÁ de la cláusula — en vez de fiarse de `count(*)`, comparó **md5 del contenido completo** de `user` y `session` entre producción y la BD restaurada: content-identical. Restauró sobre una BD que creó vacía él (0 tablas). Ejecutó el control negativo del `exit 1` por su cuenta. Y **dejó documentado su propio error metodológico**: su primer control negativo del cron dio exit 0 porque `env -i` limpia el entorno **pero no el cwd**, y su shell estaba dentro del repo; repetido desde `$HOME` da 127. Restauró el script mutado con `cp` desde copia previa y **jamás con `git checkout`** — habría borrado la tarea entera, que vivía sin commitear.

**Decisión de alcance**: `COUNT_SQL` pasa a comparar todos los schemas de usuario, no solo `public`. Un restore que perdiera `drizzle.__drizzle_migrations` enseñaría las 3 tablas de negocio intactas pero le diría a Drizzle que ninguna migración se aplicó, y el siguiente arranque intentaría re-ejecutarlas todas. **El «3 tablas» del planning y el «4» de las salidas NO se contradicen**: 3 de negocio + la de migraciones; `pg_restore --list` cuenta TABLE DATA de todos los schemas.

**Gate**: `pnpm gate` verde (**632 tests / 60 ficheros**, desde 626). Sin `test:e2e`: no cambió superficie de navegador. Re-gate post-commit verde.
**Coste**: $0.
**NO VERIFICADO (límite explícito, no defecto)**: que el **daemon de cron dispare** a las 03:30 — nunca ha disparado, la primera vez real es mañana. Lo demostrable sí está: la línea literal en entorno desnudo da exit 0 y crea dump, y **sin el `cd` da exit 127** (el `cd` es load-bearing). `verify.sh` avisa si no hay dump en 48 h. `history_entry` está a 0 filas en producción, así que esa fila de la comparación es vacua hoy — el mecanismo sí está probado que muerde (dump fraguado al que se le quitó una fila → exit 1).
**Deuda anotada (no bloquea)**: (1) la comparación es de cardinalidad/contenido por md5 en la verificación, pero el SCRIPT solo compara `count(*)` — un restore que preservara recuentos y corrompiera valores pasaría. (2) `deploy-infra.test.ts` sigue con el `stripComment` ingenuo (`replace(/#.*$/)`) mientras el nuevo es quote-aware: latente, hoy no hay `#` entre comillas en lo que lee. (3) Los dos `deploy-*.test.ts` corren bajo `jsdom` en `web:unit` solo para hacer `readFileSync` — y ahora lanzan subprocesos de shell ahí; merecen un proyecto `infra:unit` (exige paquete + config + workspace). Evidencia extra: al mover la resolución de rutas al fichero de test reventó con `The URL must be of scheme file` porque bajo jsdom el `import.meta.url` de un `*.test.ts` no es `file://`. (4) `ControlMaster` en el `ssh` de `_lib.sh` (heredada de T3.1). (5) `src` se cuenta contra producción VIVA, no contra el snapshot del dump: dirección segura (solo falsos FAIL), pero será flaky con tráfico.
**Arnés — dos desviaciones anotadas**: (a) **`/code-review` no es invocable por el modelo** (`disable-model-invocation`), así que el paso 5a se cubrió con un subagente adversarial de contexto fresco con brief de effort alto. Encontró cosas reales y confirmadas ejecutando, pero **no es el mismo pase**: si se quiere el de verdad, lo lanza el humano. Conviene decidir si la skill `dev-loop` cambia el paso 5a o si el bucle debe pedirlo al usuario. (b) **`ds-reviewer` saltado**: el diff toca `apps/web/**` solo como módulos de test/helper de node, sin superficie renderizada.
**Siguiente**: **T3.3** (E2E de fase F3) = **cierre del proyecto v1**, con parada de fin de fase y revisión de la prosa de los READMEs.
