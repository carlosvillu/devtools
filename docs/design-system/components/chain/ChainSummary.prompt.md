Compact single-line summary of a whole chain — kind badges joined by chevrons. Used in history rows and chain overviews.

```jsx
<ChainSummary kinds={["base64", "json"]} />
<ChainSummary kinds={["jwt", "json"]} size="md" />
```
