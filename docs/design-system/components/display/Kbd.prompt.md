A keyboard key cap. Used for shortcut hints — devtools is keyboard-first (focus on load, copy any step without the mouse).

```jsx
<Kbd>⌘</Kbd> <Kbd>V</Kbd>  para pegar
<Kbd>Esc</Kbd> para limpiar
```
