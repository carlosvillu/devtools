Generic surface panel: white background, subtle border + shadow, rounded 12px. Container for auth forms and content sections.

```jsx
<Card padding="lg">…login form…</Card>
<Card inset>quiet nested block</Card>
<Card hover>clickable</Card>
```

Props: `padding` sm/md/lg, `inset` (sunken, no shadow), `hover` (lift).
